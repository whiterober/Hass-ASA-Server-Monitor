# 计划：底部功能条 + 「火山计时」（Gen 首发）

> 状态：**待实施**（方案已定，等用户拍板 4 项后开工）
> 建立：2026-09-13 ｜ 对象：`dino-import.html`（前端）+ `独立后端/dino_backend.py`（锚点共享存储）
> 数据依据：`火山地震喷发规律_时间对照表_20260913.md`（Gen 实服实测）

---

## 一、需求（用户 7 项）

| # | 需求 | 落点 |
|:--:|---|---|
| 1 | 聚落列表页**底部功能条**（与**繁育条同尺寸同款式**；**有功能才显示**，无功能整条隐藏） | `#lbFuncBar` + `lbRenderFuncBar()` |
| 2 | Gen 的功能条加按钮 **「火山计时」** | `LB_SRV_FUNCS` 能力表 |
| 3 | 点击弹出**与「筛选列表」一模一样的半屏浮窗**（同 `.lb-picker-overlay` / `.lb-picker-panel`） | `lbVolcanoOpen()` |
| 4 | **所有用户都能校准锚点**（共享，不区分角色/管理员） | 后端 `/api/volcano_anchor` |
| 5 | 显示**上次校准时间** | 浮窗内 `lbFmtTs` |
| 6 | 有锚点 → 显示**下次喷发倒计时** | 前端时序推算 |
| 7 | 锚点**超过 3 个喷发周期** → **强制要求校准，不再显示倒计时** | 有效窗校验 |

---

## 二、时间规律（实测定稿，全部为**游戏时间**）

| 项 | 游戏时间 | 现实时间（÷13.57） |
|---|---|---|
| **完整循环（喷发开始→下次喷发开始）** | **472 分（7h52m）** | **34 分 48 秒** |
| 喷发持续 | 51 分 | 3 分 46 秒 |
| 喷发结束 → 震1 开始 | 75 分 | 5 分 32 秒 |
| 震1 持续 | 30 分 | 2 分 13 秒 |
| 震1 开始 → 震2 开始 | 166 分（2h46m） | 12 分 14 秒 |
| 震2 持续 | 20 分 | 1 分 29 秒 |
| 震2 开始 → 震3 开始 | 158 分（2h38m） | 11 分 39 秒 |
| 震3 开始 → 喷发开始 | 22 分 | 1 分 37 秒 |

> ⚠️ **温度与周期无关**（用户已证实）：不采集温度、不做温度预警；唯一可靠信号是游戏内横幅 + DEBUFF（需在线）。

**相位表（相对「喷发开始」= 0，游戏分钟）**：

| 相对时刻 | 事件 |
|:--:|---|
| 0 | 喷发开始 |
| +51 | 喷发结束 |
| +126 | 震1 开始 |
| +156 | 震1 结束 |
| +292 | 震2 开始 |
| +312 | 震2 结束 |
| +450 | 震3 开始 |
| **+472** | **下一轮喷发开始** |

> 校验：+450 → +472 = 22 分（与「震3→喷发 22 分」一致）；+126→+450 = 324 分（166+158 ✔）。
> 换算现实：`现实偏移 = 游戏分钟 × 60 ÷ 13.57`（例：472 → 2087 秒 ≈ 34m47s）。

---

## 三、架构（三层）

```
[前端] #lbFuncBar（照抄 #lbBreedBar）→ lbVolcanoOpen()（照抄 .lb-picker-panel 半屏浮窗）
   │  GET/POST /api/volcano_anchor
   ▼
[后端] dino_backend.py：新路由 + crafting 同款写法 → 落盘 volcano_anchor.json（与 accounts.json 同目录，tmp+replace 原子写）
   ▲
[多用户共享] 任一人校准 → 全员可见（同一锚点）
```

---

## 四、前端改动（`dino-import.html`）

### 4.1 功能条 `#lbFuncBar`（照抄繁育条，仅换 id 与内容）

```html
<div id="lbFuncBar" style="display:none;position:fixed;bottom:0;left:0;right:0;background:#16161f;border-top:1px solid #2a2a3a;padding:10px 10px;z-index:997">
  <div style="display:flex;align-items:center;gap:8px;flex-wrap:wrap">
    <span id="lbFuncTitle" style="font-size:11px;color:#888"></span>
    <span style="flex:1"></span>
    <div id="lbFuncActions" style="display:flex;align-items:center;gap:8px;flex-shrink:0"></div>
  </div>
</div>
```

- **尺寸/样式与 `#lbBreedBar` 完全一致**：`position:fixed;bottom:0;left:0;right:0;background:#16161f;border-top:1px solid #2a2a3a;padding:10px 10px`；按钮同款 `border-radius:6px;padding:4px 14px;font-size:12px`
- 层级：繁育条 998（生物速查页）／功能条 **997**（聚落列表页）——两页互斥，不冲突
- **显示规则（需求 1）**：`lbRenderFuncBar()` 内按 `LB_SRV_FUNCS[当前服]` 取能力数组；**数组为空 → `display:none`**（写法对齐 `lbRenderBreedBar` 的 `show` 判定）
- 移动端：复用现有 media query 思路（`#lbFuncBar > div{flex-wrap:wrap;justify-content:center;row-gap:6px}`）

### 4.2 能力表（需求 2）

```js
var LB_SRV_FUNCS = { Gen: ['volcano'] };            // 后续逐服扩展
var LB_FUNC_DEF = { volcano: { label:'🌋 火山计时', color:'#f97316', open:'lbVolcanoOpen' } };
```

### 4.3 半屏浮窗（需求 3）——**照抄 `lbOpenSpeciesPicker` 的壳**

| 部位 | 规格（与「筛选列表」逐字一致） |
|---|---|
| 遮罩 | `class="lb-picker-overlay"`（`position:fixed;inset:0;background:rgba(0,0,0,.9);z-index:1000001;display:flex;align-items:center;justify-content:center`），点遮罩关闭 |
| 面板 | `class="lb-picker-panel"`（`background:#1a1a24;border:1px solid #333;border-radius:8px;width:90%;max-width:640px;max-height:80vh;display:flex;flex-direction:column;overflow:hidden`） |
| 头 | `class="lb-picker-header"` + 关闭按钮 `class="lb-picker-close"` |
| 内容 | `class="lb-picker-grid"` 网格（复用 `.lb-picker-item` 卡片：`.lb-picker-ic` / `.lb-picker-name` / `.lb-picker-en`） |

浮窗内容分区：
1. **状态区**：当前相位（如「震2 进行中」）+ **下次喷发倒计时**（大字，`mm:ss`）
2. **锚点区**：上次校准时间（`lbFmtTs`）、校准人、锚点节点、剩余有效时间
3. **校准区**：节点按钮组（`喷发开始 / 喷发中 / 喷发结束 / 震1 / 震2 / 震3`）→ 点任一即用**当前时刻**写锚点；另加「我现在看到喷发开始了」快捷按钮
4. **过期区**：锚点超 3 周期 → 顶部红字提示 + 隐藏倒计时（需求 7）

### 4.4 时序推算（前端纯计算）

```js
var LB_VOLC_CYCLE_G = 472, LB_VOLC_SPEED = 13.57;   // 游戏分钟 / 倍速
LB_VOLC_PHASES = [[0,'喷发开始'],[51,'喷发结束'],[126,'震1 开始'],[156,'震1 结束'],
                  [292,'震2 开始'],[312,'震2 结束'],[450,'震3 开始'],[472,'下一轮喷发']];
// anchorMs（现实时间戳）= 校准时刻 - 所选节点的游戏分钟偏移×60/13.57×1000
// 倒计时 = anchorMs + 2087 秒 − now（按周期滚动累加，取未来最近一次）
```

- 倒计时刷新：仅浮窗打开时 `setInterval(...,1000)`，关闭即清（对齐 `lbWatchStartTimer` 模式，页面常驻零开销）

### 4.5 强制校准（需求 7）

- 有效窗 = `now − anchorMs ≤ 3 × 34m47s = 104 分 21 秒`
- 超窗：**不显示倒计时**，状态区显示「⚠️ 锚点已过期（超过 3 个喷发周期）· 请重新校准」，并把本次校准写入 `volcano_anchor.json`
- 说明：超窗后周期推算误差会累积（实服两次循环已一致，但 3 轮以上未验证），故强制人工再校准

---

## 五、后端改动（`独立后端/dino_backend.py`，3 处）

| 位置 | 内容 |
|---|---|
| 常量 | `VOLCANO_FILE = os.path.join(os.path.dirname(ACCOUNTS_FILE), 'volcano_anchor.json')` |
| 函数 | `volcano_anchor_get(server)` / `volcano_anchor_set(server, node, by, game_time)`：读/写 JSON（`tmp+os.replace` 原子写，写法对齐 `accounts` 保存 L176） |
| 路由 | `elif path.endswith('volcano_anchor'):`（GET 读 / POST 写，照 `crafting_cost` 同款分支写法） |

**API 约定**

```
GET  /api/volcano_anchor?server=Gen
     → {"ok":true,"server":"Gen","anchor":{"node":"erupt_start","anchorMs":1789300000000,"by":"角色名","ts":"2026-09-13 21:30:00"}}
POST /api/volcano_anchor   body {"server":"Gen","node":"erupt_start","by":"角色名"}
     → {"ok":true,"anchor":{...}}
```

- **权限（需求 4）**：与现有 `/api/*` 一致，**任何已登录用户均可写**（不引入管理员校验）
- 后端改完**需用户重启**（同 `crafting_cost` 流程：`tmp/_push_backend.py` 推送 + 服务器重启）

---

## 六、实施步骤

| 步骤 | 内容 | 涉及文件 | 验证 |
|---|---|---|---|
| **V1** | 后端 `volcano_anchor` 三处改动 + 推送（`tmp/_push_backend.py`）+ 用户重启 | `dino_backend.py` | `curl GET/POST` 往返一致 |
| **V2** | 功能条 `#lbFuncBar` + 能力表 + `lbRenderFuncBar()`（无功能隐藏） | `dino-import.html` | 聚落列表页显示；切到无功能服消失 |
| **V3** | 半屏浮窗（照抄 picker 壳）+ 状态/锚点/校准三区 | `dino-import.html` | 与筛选列表并排比对（尺寸/圆角/交互） |
| **V4** | 时序推算 + 倒计时 tick + 3 周期过期强制校准 | `dino-import.html` | 用已知锚点核对倒计时；改锚点后立即刷新 |
| **V5** | 部署 CF（`tmp/_deploy_cf.py`，递增 `LB_VERSION`，同步 `pwa/sw.js`） | 部署脚本 | 线上 no-store 校验版本 |
| **V6** | 三端验证（PC / 手机 / PWA） | 浏览器 | §八 验收 |

---

## 七、测试计划

| # | 步骤 | 期望 |
|:--:|---|---|
| 1 | 切到 Gen → 看底部 | 功能条出现且高度/配色与繁育条一致 |
| 2 | 切到无功能服（Isl 等） | **整条隐藏**（不占位） |
| 3 | 点「🌋 火山计时」 | 半屏浮窗弹出，遮罩/圆角/关闭与前缀筛选列表一致 |
| 4 | 点「喷发开始」校准 | 显示下次喷发倒计时 ≈ 34m47s；上次校准时间=刚刚 |
| 5 | 点「震3」校准 | 倒计时 ≈ 1m37s（震3→喷发 22 游戏分） |
| 6 | 另一账号/设备打开 | **看到同一锚点与同一倒计时**（共享生效） |
| 7 | 手工把锚点改成 2 小时前（>3 周期） | 倒计时消失 + 红字强制校准提示 |

---

## 八、验收标准

- 功能条与繁育条**同尺寸/同配色/同按钮规格**；**无功能时完全隐藏**；
- 「火山计时」浮窗与「筛选列表」**同壳同交互**（`.lb-picker-overlay/.lb-picker-panel/.lb-picker-header/.lb-picker-grid`）；
- **所有用户**都能校准，且**跨用户/跨设备共享**同一锚点；
- 显示**上次校准时间**（+校准人+节点）；
- 有锚点即显示**下次喷发倒计时**（秒级刷新）；
- 锚点**超过 3 个周期**：**不再显示倒计时**，提示强制校准；
- 不采集/不使用温度数据。

---

## 九、风险与规避

| 风险 | 规避 |
|---|---|
| 周期实为单次观测（仅 2 轮验证） | 3 周期强制校准（需求 7 本身就是兜底）；浮窗标注「数据来源：实服 2 轮实测」 |
| 服务器重启/回档导致时钟漂移 | 锚点存**现实时间戳**；过期即强制校准 |
| 多人同时校准互相覆盖 | 后端**后写覆盖**（记录 `by` + `ts`），浮窗显示校准人便于追责 |
| 后端未重启 → 接口 404 | 前端错误提示复用 `lbBpErrText` 风格（「接口未就绪（后端需重启）」） |
| 功能条与繁育条重叠 | 两页互斥 + 层级 997/998；如真同页出现，规则为「繁育条优先，功能条上移 8px」 |
| 半屏浮窗层级低于吸顶条 | 直接复用 `.lb-picker-overlay` 的 `z-index:1000001`（天然最高档） |

---

## 十、待用户拍板（4 项）

| # | 事项 | 建议 |
|:--:|---|---|
| 1 | 锚点节点粒度 | 提供 6 个节点（含「喷发中」）；**默认按钮「喷发开始」** |
| 2 | 过期阈值 | 3 个周期（≈104 分钟现实），与需求一致 |
| 3 | 后端存储 | `volcano_anchor.json` 与 `accounts.json` 同目录（**我改 + 推送，你重启**） |
| 4 | 倒计时展示 | 只显示「下次喷发」；是否附带显示当前相位与下个地震时刻（建议显示，便于行动） |

---

## 附：关键代码锚点（现成可复用）

| 用途 | 位置 |
|---|---|
| 繁育条（尺寸样式母版） | `#lbBreedBar`（body L16604）、移动端 media query L122–128 |
| 繁育条显隐逻辑范式 | `lbRenderBreedBar()` L11322（`show` 判定 + `display:block/none`） |
| 半屏浮窗壳 | `.lb-picker-overlay/.lb-picker-panel/.lb-picker-header/.lb-picker-grid`（CSS L303–318） |
| 浮窗构建范式 | `lbOpenSpeciesPicker()` L10144–10160 |
| 定时轮询范式（关窗即停） | `lbWatchStartTimer/lbWatchStopTimer` |
| 时间格式化 | `lbFmtTs()` |
| 后端 JSON 落盘范式 | `accounts` 保存 L176（`tmp + json.dump`） |
| 后端路由范式 | `elif path.endswith('crafting_cost'):`（L1304 附近） |
| 后端推送脚本 | `tmp/_push_backend.py` |
