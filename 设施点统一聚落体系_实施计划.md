# 设施点统一聚落体系 — 完整实施方案（v194）

> 2026-08-15 设计（v2：通用设施点框架，Phase 1-3 全做，未来所有设备可扩展）
> 背景：v190/v191 统一了"容器识别"层，但**聚落归属层是两套入口**（生物 vs 基因设备），床不参与中心计算 → 海潮厅主宅基因台归属失败。

---

## 1. 目标

**所有设施统一为"设施点"（facility point），统一参与聚落体系的收集/聚类/中心/归属/显示。** 不只是生物容器/床/基因设备，未来任何有坐标的设施（发电机、种植槽、门框…）都能注册接入。

## 2. 通用设施点抽象

```js
// 统一设施点
// { kind, key, x, y, z, tribeId, name, meta }
// kind: 'bio'(生物容器) | 'bed'(床) | 'gene'(基因设备) | 未来扩展
```

### 2.1 设施点来源注册表（可扩展）

```js
var LB_FACILITY_SOURCES = {
  bio:  function(abbr, data){ /* cryo 容器 + released → 设施点[]（含 count/imps/keys 元数据） */ },
  bed:  function(abbr, data){ /* structures 床类 → 设施点[]（含 name） */ },
  gene: function(abbr, data){ /* geneDevices infuser+scanner → 设施点[]（含 slots） */ }
  // 未来：'power'/'farm'/... 加一个 source 函数即可
};
```

- `kind` 决定参与阶段：`bio`=聚落核心（聚类），`bed`/`gene`=附属点（中心/归属/成立）
- 未来新设备默认按 `附属点` 参与（中心+归属），如需聚类改 meta 标记

## 3. 聚落算法重构（lbBuildSettlements）

### 3.1 流程（每服务器）

1. **收集**：调用全部 source，按部落分组收集设施点
2. **bio 聚类**：bio 设施点 DBSCAN（EPS=8000，同部落）→ 候选簇（聚落核心，结构不变）
3. **成立判定（增强）**：簇内 **bio 或 gene** 设施点附近有床（≤EPS）→ 成立聚落
   - 原：仅 bio 容器有床 → 纯基因聚落（基因台+床）也能成立
4. **床消费**：床归属聚落（已有逻辑，一张床只属一聚落）
5. **中心计算（纳入床/基因）**：聚落中心 = (bio 容器 + 本聚落床 + 本聚落基因) 坐标质心
   - 海潮厅：床3+基因台1 纳入 → 中心移到主宅 → 基因台自然归属
6. **基因归属（byGeneDevice）**：基因设备按坐标匹配聚落（同部落 ≤ ABSORB_EPS=20000）→ 建映射
7. **吸收**：离散 bio 容器同部落近聚落吸收（已有）
8. **映射**：byDinoKey / byContainer / **byGeneDevice** / **byBed** 统一建立

### 3.2 数据结构

```js
// _settlements.byServer[abbr] = {
//   settlements: [ { id, tribeId, center{x,y,z}, radius, containers[], pods, owners, name, beds[], geneKeys[] } ],
//   scattered: N
// }
// _settlements.byGeneDevice = { abbr_key: {setId, scattered, server, tribeId} }
// _settlements.byBed = { abbr_bedkey: {setId, scattered, server, tribeId} }
```

## 4. 归属统一入口

```js
// 替代 lbSettlementOf（bio）/ lbGeneSettlement（gene）
function lbFacilitySettlement(abbr, item){
  if(item.kind==='bio')  return lbSettlementOf(item.meta.rec);      // 查 byDinoKey/byContainer
  if(item.kind==='gene') return lbGeneSettlement(abbr, item);       // 查 byContainer→byGeneDevice→坐标兜底
  if(item.kind==='bed')  return byBed[abbr+'_'+item.key];           // 查 byBed
  // 兜底：坐标匹配 + 离散
}
```

- `lbGeneSettlement` 重构：**优先查 byGeneDevice**（聚落形成时建立）→ 容器 containerId → 坐标兜底
- 消除"两套入口"：全部设施统一走聚落映射

## 5. 显示统一（Phase 2）

1. **聚落按钮**：v184"纯基因聚落仅选物种显示"→ 改为"聚落有 bed 或 gene 即显示"（全部物种也显示）
2. **聚落详情**：新增"床"分组（带 name，如"主宅床"），与容器/基因设备并列
3. **基因设备详情**：已有，走统一归属后自然正确

## 6. 版本与缓存

- `_schemaVer` 8 → **9**（强制重算聚落，旧缓存失效）
- 部署 v194

## 7. 风险与回归

| 风险 | 缓解 |
|------|------|
| 聚落中心变化（含床/基因） | 回归对比全服聚落中心，确认合理；床只纳入"被消费的床" |
| 纯基因聚落新增 | 收敛：仅"有床支撑"的成立，防误报 |
| byGeneDevice 需 key 唯一 | 用后端 g.key；scanner 用 containerId 兜底 |
| 性能（床 52万条） | 按部落过滤（已有），只在聚落簇内算床 |

**回归验证清单：**
- [ ] 海潮厅主宅：基因台（slots 3）显示在聚落详情
- [ ] 全服聚落中心变化合理（无漂移）
- [ ] 聚落按钮/地图/详情正常（含纯基因聚落）
- [ ] 玩家模式/全部物种模式均正常
- [ ] 全服基因设备 scattered = 0（无本应归属却离散）

## 8. 涉及文件

- `dino-import-new.html`（唯一，前端内联）

## 9. 实施顺序

1. 备份 v193（✅ 已备份 `bak/dino-import-new_backup_v193_20260815_151816.html`）
2. 重构 `lbBuildSettlements`（设施点收集/成立/中心/映射）
3. 重构 `lbGeneSettlement` → 统一入口
4. 显示层：聚落按钮/详情/床分组
5. `_schemaVer`→9，部署 v194
6. 回归验证（海潮厅 + 全服）
7. 命名跟随新中心（v195，备份 `bak/dino-import-new_backup_v194_20260815_153336.html`）✅

## 10. 扩展指南（未来新设施接入方法，已固化到 repo 记忆）

> 任何有坐标的新设施（发电机/种植槽/门框/传送器等）接入聚落体系：

1. **注册来源**：在 `LB_FACILITY_SOURCES` 加 source 函数 → 返回设施点数组 `{kind, key, x, y, z, tribeId, name, meta}`
2. **默认附属点参与**：中心计算（坐标质心）+ 归属映射（byXXX）；需独立聚类则 meta 标记
3. **归属入口**：`lbFacilitySettlement(abbr, item)` 加 kind 分支
4. **显示**：聚落详情加对应分组
5. **成立判定**：如需支撑聚落成立（如 bed），在 lbBuildSettlements 成立判定加 kind

## 11. 完成状态（v194-v195）

- ✅ v194：设施点化（床纳入中心/byGeneDevice/byBed/详情床分组/schemaVer 9）
- ✅ v195：命名跟随新中心（做事做完整）
- ✅ 验证：海潮厅基因存储器 3 显示、全服 infuser scattered=0、聚落命名正常（36/67 匹配床名）
