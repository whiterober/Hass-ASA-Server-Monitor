# dino-import 全项目数据化报告（v3998 更新版 · 2026-09-25）

> 本文原生成于 v3997（P5~P9 迁移前） ⇒ **已就地更新至 v3998 完成状态**
> 数据来源：`tmp/_v3997_full.py`（全量扫描）+ `tmp/_v3998c_check.py`（键集校验）+ 线上 v3998 浏览器实测

---

## 一、总览

| 项 | 数值 |
|----|------|
| 前端主文件 | `dino-import.html` —— **24215 行 / 1382 KB** |
| 顶层常量 | **93 个**（数组 38 + 对象 55） |
| ✅ **已数据化** | **41 个** —— 判定名单 / 特例表类 **全覆盖**（JSON 为主源，HTML 降级兜底） |
| ⏸ 按设计保留 | **52 个** —— 参数 / 样式 / 资源映射 / 运行时（**属合理硬编码，迁移无收益**） |
| 数据文件 | **14 个**（全部就位；`dino_rules.json` 扩容至 **37.4 KB / 41 键 / 1065 条**） |
| 前端加载器 | **18 个**（`lbMatLoad` / `lbRulesLoad` 职责扩展） |
| 线上版本 | **v20260924-3998**（Version ID `bd901d08-48b4-441d-9a0f-76624bc9c3cc`） |

> 迁移模板（已验证）：**JSON 唯一源 + HTML 降级兜底 + 并集/补键合并** ⇒ 零调用点改动、加载前后行为单调安全。

---

## 二、数据源总表（14 个数据文件）

| # | 文件 | 大小 | 职责 | 加载器 | 主要消费点 |
|---|------|------|------|--------|-----------|
| 1 | `汉化/item_zh.json` | 608 KB | **物品词典**（2813 键：名/图标/kind/`quest`/`switch`/`ase`/`skin`） | `lbItemZhLoad` | 物品浮窗/容器/搜索 |
| 2 | `汉化/facility_zh.json` | 19 KB | 设施表（225 键：名/图标） | `lbFacilityZhLoad` | 聚落设施/容器组 |
| 3 | `汉化/facility_official.json` | 171 KB | 容器官中名（PO 溯源） | `lbFacilityOfficialLoad` | 容器卡片 |
| 4 | `汉化/icon_zh_map.json` | 282 KB | 图标 → 中文名 | （并入物品/设施链） | 图标反查 |
| 5 | `汉化/icons2.json` | 627 KB | 图标 URL 表 | `lbLoadIconList` | 图标加载 |
| 6 | `汉化/itemstats.json` | 49 KB | 物品属性 | `lbItemStatsLoad` | 物品详情 |
| 7 | `lb_material.json` | 58 KB | 材质排序 + `asePurge`(57) + `hideItems`(5) + `authStructs`(189) | `lbMatLoad` | 建筑栏排序 / 隐藏 / 构件归类 |
| 8 | **`dino_rules.json`** | **37.4 KB** | **规则名单唯一数据源（41 键 / 1065 条）**：设施图标特例 116 ｜ 生物判定 10 名单 321 ｜ 物品判定族 70 ｜ 设施族 62 ｜ 分类图鉴族 121 ｜ 词典特例族 183 ｜ 容器大表 192 | `lbRulesLoad` | 生物归类 / 设施图标 / 物品判定 / 容器容量 |
| 9 | `icon_anti_color.json` | 2 KB | 图标反色表 | `lbLoadAntiColors` | 图标滤镜 |
| 10 | `tmp/dino_prefix_zh.json` | 0.5 KB | 生物前缀中文 | `lbLoadPrefixZh` | 生物名 |
| 11 | `tmp/dino_colors.json` | 13 KB | 颜色表 | `lbLoadColors` | 颜色渲染 |
| 12 | `tmp/gene_traits_zh.json` | 4 KB | 基因特性中文 | （并入链） | 基因栏 |
| 13 | `values.json`（引用目录） | 2588 KB | 属性值域 | （启动链） | 属性计算 |
| 14 | `ASA-values.json`（引用目录） | 1098 KB | ASA 属性值域 | （启动链） | 属性计算 |

---

## 三、已数据化清单（41 个常量 · v3993 → v3998）

### （一）v3993 ~ v3996：15 个（任务物品 / 隐藏 / 构件 / 图标特例 / 生物 10 名单）

| 版本 | 原常量 | 规模 | 新数据源字段 | 前端机制 |
|------|--------|:---:|-------------|---------|
| v3993 | `LB_QUEST_KEYS` | 7 | `item_zh.json` → `quest:1` | `lbQuestIdx` O(1) 索引 + 双查 |
| v3995·P1 | `LB_HIDE_ITEMS` | 5 | `lb_material.json` → `hideItems` | `lbIsHiddenItem` 双查 |
| v3995·P2 | `LB_AUTH_STRUCT_BASE` / `_EXTRA` | 189 | `lb_material.json` → `authStructs` | `lbIsAuthStruct` 双查 |
| v3996·P3 | `LB_FACILITY_ICON_SPECIAL` | 116 | `dino_rules.json` → `facilityIconSpecial` | `lbRulesMerge` 对象覆盖 |
| v3996·P4 | 生物判定 10 名单 | 321 | `dino_rules.json` → 10 字段 | `lbRulesMerge` 并集追加 |

### （二）v3997：26 个（物品判定族 / 设施族 / 分类图鉴族 / 词典特例族 / 容器大表）

| 批次 | 原常量 | 规模 | 新数据源字段 |
|------|--------|:---:|-------------|
| **P5** 物品判定族 | `LB_NOQ_WEAPON` / `LB_ALLOW_ZERO_QTY` | 55 / 15 | `noqWeapon` / `allowZeroQty` |
| **P6** 设施族 | `LB_PUBFAC_PREFIX` / `LB_FAC_KEY_ALIAS` / `LB_FAC_RENAMEABLE` | 9 / 12 / 41 | `pubfacPrefix` / `facKeyAlias` / `facRenamable` |
| **P7** 分类图鉴族 | `LB_SPECIAL_KEYWORDS` / `LB_BOSS_TROPHY` / `LB_FOOD_WL` / `LB_EGG_MAP` / `LB_EGG_LV` / `LB_DYE_FAM_FIX` / `LB_NORMAL_WHITELIST` / `LB_FULL_BREED_EXTRA` | 18/11/18/39/6/14/14/1 | 8 个同名字段 |
| **P9** 词典特例族 | `LB_SPECIAL_NAMES` / `LB_ICON_SPECIAL` / `LB_ZH_SYNONYM` / `LB_CLS_SUFFIX` / `LB_CLS_PREFIX` / `LB_SUFFIX_PREFIX` | 124/41/11/4/3/4 | 6 个同名字段 |
| **P8** 容器大表 | `LB_CONTAINER_DISP`（含 `_cls`）/ `LB_CONT_CAP` 系列 6 表 | 10+24 / 154 | `containerDisp`+`containerDispCls` / `contCap` 系列 6 字段 |

**3 项特殊处理**：① 正则字段（`LB_BOSS_TROPHY`、`_cls`）数据以 **`p`（模式）/`f`（标志）** 承载，加载时 `new RegExp` 重建（按 `String(re)` 去重）；② `_LB_EGG_KEYS`（长键优先缓存）合并后**重建**；③ `LB_FAC_RENAMEABLE_N`（惰性缓存）置 `null` 刷新。

---

## 四、剩余未迁移（52 个 · **全部按设计保留**）

> 判据：这些是「游戏机制常量 / 样式参数 / 资源映射 / 运行时状态」—— 游戏机制本身稳定，**不因新内容出现而失效** ⇒ 迁移无收益。

| 类 | 代表常量 |
|----|---------|
| 配方/计算系数 | `LB_RECIPE_COEF`(30) ｜ `LB_ROUTE_*`(19) ｜ `LB_DYE_FAM_RANGE` |
| 分组/样式定义 | `LB_INV_GRP`(36) ｜ `LB_TYPE_BADGE`(27) ｜ `LB_BP_STAGE_COL` ｜ `LB_CONTENT_KINDS` |
| 资源映射 | `LB_MAP_INFO`(65) ｜ `LB_MDI_SVG`(40) ｜ `LB_SERVER_MDI` ｜ `LB_FALLBACK_SERVERS`(33) |
| 机制前缀/枚举 | `LB_ITEM_PRES`(15) ｜ `LB_BASE_MOD` ｜ `LB_BASE_WYV_EL` ｜ `LB_BREED_KEYS` |
| 序/区间 | `LB_EGG_LV_ORDER` ｜ `LB_CONTAINER_ORDER` ｜ `LB_DYE_FAM_ORDER` |
| 运行时/配置 | `LB_VOLC`（**可被就地赋值，不可迁**）｜ `LB_WATCH_DICT_IDX` ｜ `LB_CRYO_WAIT_WHO` ｜ `LB_ALLI_BLOCK` |

---

## 五、策略性硬编码复查（结论：**合理，无需迁移**）

| 类型 | 数量 | 样本 | 定性 |
|------|------|------|------|
| 类名全等比较 | 27 处 | `k === 'DinoClass'` | **INI 字段名解析**（数据格式常量）✓ |
| 类名 indexOf | 14 处 | `'DinoTamedInventoryComponent'` | **游戏机制常量** ✓ |
| 类名前缀正则 | 27 处 | `/^PrimalItem[A-Za-z]*_/` | **归一化机制** ✓ |
| Skin/前缀正则 | 11 处 | `/^Skin_/`、`/^StructureSkin_/` | **皮肤判定机制** ✓ |
| 魔法数字 | 7 处 | `length > 10000`、`length>80` | **阈值** ✓ |

⇒ 未见隐藏的"名单型"硬编码（所有名单已在前四节盘点）。

---

## 六、执行记录（v3997 / v3998）

### P5~P9 迁移过程要点

| # | 事件 | 处置 |
|---|------|------|
| 1 | 提取器 10 名单 0 条 | 根因：跨行块注释清洗 `re.sub(r'/\*.*?\*/', re.S)` 因 `/*` 嵌套错配（116 vs 122）**吞掉大段代码** ⇒ 改**单行清洗** |
| 2 | 2 名单仍 0 条 / 2 名单异常大 | 根因：`};` 不在行首 + 键无引号 ⇒ 结尾 `\};`（不限行首）；键正则双分支 |
| 3 | 键集校验 | 15 名单 HTML↔JSON **全 0 缺失** + 值抽查全对（Vault 350 / Refrigerator 80 / eggMap / specialNames …） |
| 4 | 上线后发现 `foodWl` 18→36 | 根因：通用 `_union` 的 `indexOf` 对**数组元素是引用比较** ⇒ 改**深去重**（按 `[0]` 子串）⇒ **v3998** 修复 |

### 线上验证（v3998 浏览器实测）

`foodWl` **18** ✓ ｜ `noq` 55 ｜ `allowZero` 15 ｜ `contCap` 128 ｜ `specialNames` 124 ｜ `eggMap` 39 ｜ `eggKeys` 39（重建）｜ `bossRe` `/Broodmother|育母蜘蛛/i`（RegExp 重建 ✓）｜ `_cls` 24（去重命中）｜ `pubfacPrefix` 9 ｜ `facRenamable` 41
功能抽查：`Refridgerator→Refrigerator` ✓ ｜ `Vault=350` ✓ ｜ `lbIsNoQualityItem(WeaponC4)=true` ✓

### 部署版本链

| 版本 | Version ID | 内容 |
|------|-----------|------|
| v3996 | `29fb3867` | P3/P4（图标特例 + 生物 10 名单） |
| v3997 | `60625386` | P5~P9（26 名单 / 1065 条） |
| **v3998** | **`bd901d08`** | `foodWl` 深去重修复（当前线上） |

---

## 七、产物与备份

- 扫描：`tmp/_v3997_full.py` → `tmp/_v3997_out.txt`
- 提取：`tmp/_v3998_extract.py`（含 2 轮修复）｜ 校验：`tmp/_v3998c_check.py`
- 前端改造：`tmp/_v3999_apply.py` ｜ 自检：`tmp/_v3999f_check.py` ｜ 探测：`tmp/_probe3998.py`
- 备份：`bak\v3995_20260925_181837\` ｜ `bak\v3996_20260925_181955\` ｜ `bak\v3997_20260925_182722\` ｜ `bak\v3998_*\` ｜ 本报告更新前版本：`bak\report_20260925_183412\`
