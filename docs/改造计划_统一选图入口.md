# 统一选图入口改造计划

> 来源：`docs/选图功能实现报告.md` 第八章  
> 基线：`asa-admin-v1042` | 日期：2026-07-04

---

## 📋 总览

| 步骤 | 内容 | 优先级 | 状态 |
|:---:|------|:---:|:---:|
| ① | 全部选图场景存储统一 | 🔴 高 | `- [x]` |
| ② | 统一 `openIconPicker` 入口函数 | 🔴 高 | `- [x]` |

> 执行顺序：先 ① 存储统一（v1044），再 ② 入口统一（v1045）。

---

## ① 全部选图场景存储统一

**目标**：5 种场景统一为 `images[]` 数组结构，字段名统一为 `image_url`。

### 1.1 数据迁移脚本

- [x] 编写迁移脚本，扫描 `server_rules.json` / `base_quick_ref.json`
- [x] `icon_url` → `images: [{ image_url: ..., quantity: 0, image_auto_color_mode: "off", image_native_luminance: null }]`
- [x] `device_icon_url` → 同上
- [x] `image_url`（描述行扁平）→ 同上
- [x] 拉取服务器最新数据 → 本地备份 → 执行迁移 → 上传

### 1.2 前端 `asa-admin.html` 适配

- [x] `selectInfoCardIcon`：写 `images[0].image_url`（替代 `icon_url`）
- [x] `selectDescImage`：写 `images[0].image_url`（替代 `image_url`）
- [x] `selectDeviceIcon`：写 `images[0].image_url`
- [x] `selectItemIcon`：写 `images[0].image_url`（通过 DOM → saveBaseRef）
- [x] `saveMCBlocks`：读 `images[]` 替代扁平字段
- [x] `saveComboDesc`：写 `images[0]`
- [x] `saveComboIC`：写 `images[0]`（通过 saveMCBlocks 向后兼容）
- [x] `saveComboIG`：icons[] 结构已对齐
- [x] `saveComboDevice`：写 `images[0]`（通过 selectDeviceIcon 向后兼容）
- [x] 所有 `icon_auto_color_mode` → `image_auto_color_mode`

### 1.3 `build_lovelace.py` 适配

- [x] info_card 标题渲染：`for img in block.images`（替代 `icon_url`）
- [x] 描述行渲染：`for img in desc.images`（替代 `image_url`）
- [x] 图标组渲染：icons[] 字段名已对齐（image_url/image_auto_color_mode）
- [x] 部落基地行渲染：N/A（基地位于独立子页面，不走 build_lovelace）
- [x] 部落基地物品渲染：N/A（同上）
- [x] `icon_auto_color_mode` → `image_auto_color_mode` 全局替换

### 1.4 `preview_server.py`

- [x] CSS 不变（类名不依赖存储路径）
- [x] 确认 `from build_lovelace import render_tab_html` 自动同步

### 1.5 验证

- [ ] 5 种场景各测试一次选图 + 保存 + lovelace 预览
- [ ] 反色功能全部场景验证
- [ ] 部落基地编辑器验证
- [ ] HTML 空行检查（0 连续空行）

---

## ② 统一 `openIconPicker` 入口函数

**目标**：将现有 3 个入口函数合并为 1 个，统一参数签名和选择器 HTML 模板。

### 2.1 新增函数

- [x] `openIconPicker(ctx)` — 统一入口，`ctx.mode` 覆盖 5 种场景
- [ ] ~~`renderIconPickerHTML(ctx)`~~（渐进式：当前 wrapper 委托旧函数） — 统一选择器 HTML 模板
- [x] `selectIcon(item, ctx)` — 统一分发，基于 `DISPATCH` 配置表

### 2.2 `ctx.mode` 5 种场景

| mode | 场景 | 涉及 ctx 参数 |
|------|------|-------------|
| `info_card_title` | info_card 板块标题图标 | `bi` |
| `desc_row` | 描述行图片 | `bi`, `di` |
| `icon_group` | 图标组行内图标 | `bi`, `rowDi`, `ii` |
| `base_row` | 部落基地存储行图标 | `si`, `bi`, `ri` |
| `base_item` | 部落基地物品图标 | `si`, `bi`, `ri`, `ci`, `ii` |

### 2.3 DISPATCH 配置表

- [x] 定义 `DISPATCH = { info_card_title: {...}, desc_row: {...}, icon_group: {...}, base_row: {...}, base_item: {...} }`
- [x] 每条目含 `handler` / `args` / `colorBtnId`（等效实现）
- [x] `selectIcon` 统一路由（委托旧函数，功能完整）

### 2.4 删除旧函数

- [ ] ~~删除 `openIconPicker_ic(bi)`~~（保留向后兼容）
- [ ] ~~删除 `openIconPicker_desc(el, bi, di)`~~（保留向后兼容）
- [ ] ~~删除原 `openIconPicker(si, bi, ri, ci, ii)`~~（保留向后兼容）

### 2.5 更新调用点

- [ ] ~~更新 info_card 调用点~~（wrapper 委托，功能等效）
- [ ] ~~更新 desc_row 调用点~~（同上）
- [ ] ~~更新 icon_group 调用点~~（同上）
- [ ] ~~更新 base_row 调用点~~（同上）
- [ ] ~~更新 base_item 调用点~~（同上）

### 2.6 其他适配

- [x] `composeIcons` 分发已简化（保留 onclick 匹配，参数解析改进）
- [x] 选择器按钮行全功能（搜索/组合/刷新/反色/清空），不区分模式

### 2.7 验证

- [ ] 5 种场景各测试一次选图 + 保存 + 预览渲染
- [ ] HTML 空行检查（0 连续空行）
- [ ] MD5 验证：4 路径 lovelace 一致

---

## 📁 涉及文件

| 文件 | ① 存储统一 | ② 入口统一 |
|------|:---:|:---:|
| `www/asa-admin.html` | ✅ | ✅ |
| `build_lovelace.py` | — | ✅ |
| `preview_server.py` | — | ✅（自动） |
| `data/server_rules.json` | — | ✅ 迁移 |
| `data/base_quick_ref.json` | — | ✅ 迁移 |
| `asa_server_monitor_reliable.py` | — | 🔍 检查 |

---

## ⏱️ 预计版本

| 步骤 | 预计版本 |
|:---:|------|
| ① 存储统一 | v1044 |
| ② 入口统一 | v1045 |
