# dino-import 全项目数据化报告（v3997 · 2026-09-25）

> 用户指令：「全面复查全项目漏网之鱼以及给一份全项目数据化报告」
> 数据来源：`tmp/_v3997_full.py` 全量扫描（原始输出 `tmp/_v3997_out.txt`）

---

## 一、总览

| 项 | 数值 |
|----|------|
| 前端主文件 | `dino-import.html` —— **24215 行 / 1382 KB** |
| 顶层常量 | **93 个**（数组 38 + 对象 55） |
| 已数据化（v3993~v3996） | **15 个**（判定名单/特例表） |
| 判定名单类未迁移 | **≈19 个**（详见第四节） |
| 参数/样式/资源映射类 | ≈59 个（**正常硬编码，建议保留**） |
| 数据文件 | **14 个**（全部就位 ✓） |
| 前端加载器 | **18 个**（含 v3996 新增 `lbRulesLoad`） |
| 部署管道 | 静态表 **14/14**（`_sync_repack2.py`） |

---

## 二、数据源总表（14 个数据文件）

| # | 文件 | 大小 | 职责 | 加载器 | 主要消费点 |
|---|------|------|------|--------|-----------|
| 1 | `汉化/item_zh.json` | 608 KB | **物品词典**（2813 键：名/图标/kind/`quest`/`switch`/`ase`/`skin`） | `lbItemZhLoad` | 物品浮窗/容器/搜索 |
| 2 | `汉化/facility_zh.json` | 19 KB | 设施表（225 键：名/图标） | `lbFacilityZhLoad` | 聚落设施/容器组 |
| 3 | `汉化/facility_official.json` | 171 KB | 容器官中名（PO 溯源） | `lbFacilityOfficialLoad` | 容器卡片 |
| 4 | `汉化/icon_zh_map.json` | 282 KB | 图标 → 中文名 | （并入物品/设施链） | 图标反查 |
| 5 | `汉化/icons2.json` | 627 KB | 图标 URL 表 | `lbLoadIconList` | 图标加载 |
| 6 | `汉化/itemstats.json` | 49 KB | 物品属性 | `lbItemStatsLoad` | 物品详情 |
| 7 | **`lb_material.json`** | 58 KB | 材质排序 + `asePurge`(57) + **`hideItems`(5)** + **`authStructs`(189)** | `lbMatLoad` | 建筑栏排序 / 隐藏 / 构件归类 |
| 8 | **`dino_rules.json`** | 14 KB | **设施图标特例(116) + 生物判定 10 名单(321)** | **`lbRulesLoad`** | 生物归类 / 设施图标 |
| 9 | `icon_anti_color.json` | 2 KB | 图标反色表 | `lbLoadAntiColors` | 图标滤镜 |
| 10 | `tmp/dino_prefix_zh.json` | 0.5 KB | 生物前缀中文 | `lbLoadPrefixZh` | 生物名 |
| 11 | `tmp/dino_colors.json` | 13 KB | 颜色表 | `lbLoadColors` | 颜色渲染 |
| 12 | `tmp/gene_traits_zh.json` | 4 KB | 基因特性中文 | （并入链） | 基因栏 |
| 13 | `values.json`（引用目录） | 2588 KB | 属性值域 | （启动链） | 属性计算 |
| 14 | `ASA-values.json`（引用目录） | 1098 KB | ASA 属性值域 | （启动链） | 属性计算 |

---

## 三、已数据化清单（v3993 → v3996，共 15 个常量）

| 版本 | 原常量 | 规模 | 新数据源字段 | 前端机制 |
|------|--------|------|-------------|---------|
| v3993 | `LB_QUEST_KEYS` | 7 | `item_zh.json` → `quest:1` | `lbQuestIdx` O(1) 索引 + 双查 |
| v3995·P1 | `LB_HIDE_ITEMS` | 5 | `lb_material.json` → `hideItems` | `lbIsHiddenItem` 双查 |
| v3995·P2 | `LB_AUTH_STRUCT_BASE` / `LB_AUTH_EXTRA` | 189 | `lb_material.json` → `authStructs` | `lbIsAuthStruct` 双查 |
| v3996·P3 | `LB_FACILITY_ICON_SPECIAL` | 116 | `dino_rules.json` → `facilityIconSpecial` | `lbRulesMerge` 对象覆盖 |
| v3996·P4 | 生物判定 **10 名单** | 321 | `dino_rules.json` → 10 字段 | `lbRulesMerge` 并集追加 |

**共同机制**：JSON 为唯一源（可扩展）；HTML 硬编码降级为兜底 ⇒ 加载前后行为单调安全、零调用点改动。

---

## 四、剩余未迁移（分级）

### 🔴 判定名单类（≈19 个，有「新内容漏网」风险，建议后续迁移）

| 常量 | 规模 | 用途 | 风险 |
|------|------|------|------|
| `LB_CONTAINER_DISP` | 161 | 容器显示名映射 | 新容器漏 |
| `LB_CONT_CAP` 系列（6 表） | ≈159 | 容器容量/别名/共享/装备 | 新容器漏 |
| `LB_SPECIAL_NAMES` | 124 | 生物显示名特例 | 新生物漏名 |
| `LB_NOQ_WEAPON` | 55 | 无品质武器判定 | 新武器漏 |
| `LB_FAC_RENAMEABLE` | 41 | 可改名设施 | 新设施漏 |
| `LB_ICON_SPECIAL` | 41 | 生物图标特例 | 新生物漏图标 |
| `LB_EGG_MAP` | 39 | 蛋图鉴映射 | 新蛋漏 |
| `LB_FOOD_WL` | 36 | 可喂白名单 | 新食物漏 |
| `LB_SPECIAL_KEYWORDS` | 18 | 精英/特殊生物关键词 | 新变体漏 |
| `LB_EGG_LV` | 18 | 蛋等级 | 新蛋漏 |
| `LB_ALLOW_ZERO_QTY` | 15 | **零数量放行**（与 hideItems 同族） | 新物品漏 |
| `LB_DYE_FAM_FIX` | 14 | 染料族修正 | 新染料漏 |
| `LB_NORMAL_WHITELIST` | 14 | 普通白名单 | 新生物漏 |
| `LB_FAC_KEY_ALIAS` | 12 | 设施键别名 | 新设施漏 |
| `LB_ZH_SYNONYM` | 11 | 中文同义词 | 新词漏 |
| `LB_BOSS_TROPHY` | 11 | Boss 战利品 | 新 Boss 漏 |
| `LB_PUBFAC_PREFIX` | 9 | 公共设施前缀 | 新前缀漏 |
| `LB_CLS_SUFFIX`+`LB_CLS_PREFIX`+`LB_SUFFIX_PREFIX` | ≈11 | 类名后缀/前缀翻译 | 新变体漏 |
| `LB_VOLC` / `LB_FULL_BREED_EXTRA` / `LB_WATCH_DICT_IDX` | 16/1/12 | 火山计时/繁育/观测 | 小规模 |

### 🟢 参数/样式/资源映射类（≈59 个，**正常硬编码，建议保留**）

`LB_RECIPE_COEF`(配方系数) ｜ `LB_INV_GRP`(分组样式) ｜ `LB_MAP_INFO`(地图资源) ｜ `LB_SERVER_MDI` ｜ `LB_MDI_SVG` ｜ `LB_TYPE_BADGE`(角标样式) ｜ `LB_ROUTE_*`(路径计算) ｜ `LB_BASE_MOD`/`LB_BASE_WYV_EL`(变体前缀机制) ｜ `LB_ITEM_PRES`(类名前缀枚举) ｜ `LB_CONTENT_KINDS`/`LB_ITEM_RANK_KINDS`(分类定义) ｜ `LB_EGG_LV_ORDER`/`LB_DYE_FAM_ORDER`/`LB_DYE_FAM_RANGE`(序/区间) ｜ `LB_BREED_KEYS`(字段名) ｜ `LB_FALLBACK_SERVERS`(服务器兜底) ｜ `LB_ALLI_BLOCK`(配置) ｜ `LB_CRYO_WAIT_WHO`(运行时)

> 判据：这些是「**游戏机制常量 / 样式参数 / 资源映射**」—— 游戏机制本身稳定，不因新内容出现而失效 ⇒ 迁移无收益。

---

## 五、策略性硬编码复查（结论：**合理，无需迁移**）

| 类型 | 数量 | 样本 | 定性 |
|------|------|------|------|
| 类名全等比较 | 27 处 | `k === 'DinoClass'` | **INI 字段名解析**（数据格式常量）✓ |
| 类名 indexOf | 14 处 | `'DinoTamedInventoryComponent'` | **游戏机制常量** ✓ |
| 类名前缀正则 | 27 处 | `/^PrimalItem[A-Za-z]*_/` | **归一化机制** ✓ |
| Skin/前缀正则 | 11 处 | `/^Skin_/`、`/^StructureSkin_/` | **皮肤判定机制** ✓ |
| 魔法数字 | 7 处 | `length > 10000`、`length>80` | **阈值** ✓ |

⇒ 未见隐藏的"名单型"硬编码（所有名单已在前四节盘点）。

---

## 六、后续迁移建议（待用户裁定）

| 批次 | 内容 | 建议数据源 |
|------|------|-----------|
| **P5**（物品判定族，紧接 P1） | `LB_ALLOW_ZERO_QTY`(15) + `LB_NOQ_WEAPON`(55) | `dino_rules.json`（新增 `allowZeroQty` / `noqWeapon`） |
| **P6**（设施/建筑族） | `LB_PUBFAC_PREFIX`(9) + `LB_FAC_KEY_ALIAS`(12) + `LB_FAC_RENAMEABLE`(41) | `dino_rules.json` |
| **P7**（分类/图鉴族） | `LB_SPECIAL_KEYWORDS`(18) + `LB_BOSS_TROPHY`(11) + `LB_FOOD_WL`(36) + `LB_EGG_MAP`(39) + `LB_EGG_LV`(18) + `LB_DYE_FAM_FIX`(14) | `dino_rules.json` |
| **P8**（大表族） | `LB_CONTAINER_DISP`(161) + `LB_CONT_CAP` 系列(≈159) | 独立 `dino_containers.json` |
| **P9**（词典特例族） | `LB_SPECIAL_NAMES`(124) + `LB_ICON_SPECIAL`(41) + `LB_ZH_SYNONYM`(11) + `LB_CLS_*`(≈11) | `dino_rules.json` 或 `dino_prefix_zh.json` 模式 |

> 迁移模板（已验证）：**JSON 唯一源 + HTML 降级兜底 + 并集合并** ⇒ 零调用点改动、行为单调安全。

---

## 七、产物

- 扫描脚本：`tmp/_v3997_full.py`
- 原始输出：`tmp/_v3997_out.txt`（149 行）
- 本报告：`_全项目数据化报告_v3997.md`
