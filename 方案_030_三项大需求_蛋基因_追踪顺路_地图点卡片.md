# 方案：三项大需求（蛋基因筛选 / 追踪顺路 / 分布地图点卡片）

> 版本 v1.0（2026-09-05）· 勘察基准：dino-import v20260904-753
> 状态：方案（未实施）。改动集中在 `dino-import.html`（单文件），每项独立成版本部署。

---

## 需求 1：可偷蛋龙基因筛选没反映蛋的基因

### 现象
速查基因筛选选某基因后，可偷蛋龙（飞龙火/毒/电、岩龙、熔岩龙、恐爪龙等）巢里带基因的蛋不命中；「野生蛋最高」为空；但蛋数据实测**确实带基因**（Ast 飞龙火/毒/电 8 蛋全带、Val 恐爪龙、Gen 熔岩龙均带）。

### 根因（勘察实证，三环叠加）
1. **蛋物种折叠**：`lbEggToRec`(L4621) 把蛋 `dinoClass` 取 species 短名并去 `_Fertilized` → 飞龙火/毒/电/冰全部折叠成 `Wyvern`，变体只存 `itemClass`（L4656 基因已归一读入，但物种已丢）。
2. **模糊匹配撞车**：`findSpeciesByClass`(L448) 短名按"首匹配/包含"模糊匹配，实测 `Wyvern`→Corrupted Wyvern、`Deinonychus`→STA 变体、`Rex`→Corrupted Rex 抢占。
3. **严格相等过滤**：`lbSpeciesMatch`(L477) 要求选中物种对象与蛋对象相同 → 选中 `Wyvern_Character_BP_Fire_C` ≠ 折叠 `Wyvern`（Corrupted）→ 带基因巢蛋被 `wildEggs` 排除 → 基因筛选（野生组 L10324）与「野生蛋最高」全空。
- 对照组：普通低温球 `dinoClass`=完整蓝图类名精确命中，链路正常 → **蛋与普通龙的根本差异就在物种还原**。

### 方案（推荐 1+3，可选 2 加固）
1. **蛋物种还原**：`lbEggToRec` 用 `itemClass`（如 `Egg_Wyvern_Fertilized_Fire_C` / `Egg_Deinonychus_Fertilized_C`）查物种表还原**完整变体 dinoClass**（`Wyvern_Character_BP_Fire_C`），蛋保留正确变体（飞龙 4 系、恐爪龙、岩龙、熔岩龙等全覆盖）。
2. **短名防抢占**（兜底）：`findSpeciesByClass` 匹配短名前先做"精确短名 → 主物种"归一表，并排除 Corrupted/变体抢占（命中完整类名优先）。
3. **蛋匹配兜底**：`lbSpeciesMatch` 对 `source==='egg'` 增加 `itemClass` 归属匹配（防极端未覆盖变体）。

### 影响面 / 回归
- 影响：可偷蛋追踪（偷蛋列表）、「野生蛋最高」、基因筛选野生组、蛋归属标签。
- 回归：非偷蛋（低温球/容器蛋）基因/归属不受影响；验证 Ast/Val/Gen 各偷蛋变体基因筛选命中。

### 验证
浏览器 sim 数据：选飞龙（火）→ 基因筛选勾选蛋上基因 → 野生蛋/巢蛋命中出现；Ast/Gen/Val 逐变体抽查。

---

## 需求 2：追踪最近 → 起点一次性顺路路线

### 现状与问题
- 自动切换：龙丢失后 `lbHandleTrackLost` → `lbNearestWild` 每步**从当前位置**贪心选最近 → 第 2 点按第 1 点出发算，路径不顺（来回绕）。
- 手动「下一只」`lbTrackNext`(L9729)：按列表径向顺序固定，非路径最优。
- 用户约束：**停止追踪前保持固定路线（性能），但一开始算出的切换序列必须最顺路**。

### 方案：启动时一次性 TSP 近似排序成固定 route
1. 新函数 `lbBuildTrackRoute(cands, fx, fy)`：
   - 输入：起点坐标 + 候选点集合（当前物种/服务器可追踪野生）；
   - 算法：**最近邻**（从起点反复取最近未访问点）生成初始序列 → **2-opt 局部优化**（限定迭代 ≤ 5000 或计时 ≤50ms 防卡顿，仅对点少时全优化；点多 >300 时仅最近邻+少量 2-opt）。
2. 追踪启动入口统一调用：启动/续接（`lbTrackDino`、`lbTrackNext`、断线自动恢复）时若 `LB._trackRoute` 为空则构建一次；行进中**不重排**（性能约束）；到点自动切下一个按 `route` 顺序；手动下一只也走 `route`。
3. 状态持久化：`lbTrackPersistSave/Load`(L2141/2154) 增加 `route` 字段（停止追踪清除）。
4. 现有 `lbNearestWild` 改为仅用于「首次选第 1 个点」（或保留作为 route 构建的第一步），切换逻辑改读 route。

### 改动落点
- 新增：`lbBuildTrackRoute`（放追踪函数区 L9500 附近）。
- 修改：`lbHandleTrackLost`、`lbTrackNext`、`lbTrackDino`（启动构建）、`lbTrackPersistSave/Load`、停止时清 route。

### 验证
- sim/实服多目标场景：A→B→C 顺序手动画轨迹，核对总路程（优化前后对比：绕路 vs 顺路）。
- 性能：200/500/1000 候选点构建耗时计时（≤100ms 目标）。

---

## 需求 3：分布地图上每只龙点出卡片 + 追踪按钮

### 目标界面（术语澄清）
勘察确认代码无「发布地图」字样；最贴近并满足「每只龙 + 追踪」的是主界面 **「🗺 分布地图」`lbWildDistMap`(L10678)**：每服一张地图、每点=**一只野生龙**（当前物种 `_wildCache`），追踪只对野生有意义 → 以此为目标。
> 若另指"放生(released)离散点"（B：`lbSettleBtnHtml` scattered 橙点 L5956），每点=一只放出龙，属自己龙无野生追踪语义；可加"点击弹卡"但追踪按钮不适用（方案 B 附注）。

### 现状
- 分布地图点循环 L10699-10709：普通点纯静态 `<div>`（黄点/最高级菱形），**无 onclick**。
- 仅每服最高级那只：包装 rec 入 `LB._breedCands`(L10711-10719) → `topCard=lbDinoHTML(rec)`(L10730) + 追踪按钮 `lbTrackWildFrom`(L10729)。
- 现成可复用：弹层 `#lbBreedOverlay`/`lbBreedOverlayClick`、单只野生卡 `lbOpenWildMap(idx)`(L10770)（地图+黄点+LL 坐标+`lbDinoHTML`）、`lbDinoHTML(r,{trackBtn})`(L4443 支持内嵌自定义按钮)、追踪 `lbTrackWildFrom(server,idx,ev,dinoData)`(L9592 支持内联龙 JSON)。

### 方案 A（主）：分布地图每点可点
1. 点循环给每个点 `onclick` → 新函数 `lbOpenWildDot(abbr, idx, wJson)`：
   - 包装 rec（复用 L10711-10719 rec 构造）→ `LB._breedCands` 对应位 + `lbOpenWildMap(idx)` 弹单只野生卡（含地图标位）。
2. 卡内嵌追踪按钮：调 `lbDinoHTML` 时给 `opts.trackBtn`（现成支持）或复用 topCard 同款「追踪」按钮 `lbTrackWildFrom`（内联 dinoData 模式，防重建索引失效）。
3. 事件绑定：点数据闭包传 `w` 对象即可（点数=当前物种野生数，可控；无需事件委托，必要时再做）。
4. 性能：当前物种单服点数有限（几百内），每次 onClick 惰性构造卡，无预渲染负担。

### 方案 B（可选）：放生/离散橙点同样可点
- `lbSettleBtnHtml` scattered 橙点循环给每点绑定 → 弹该放生龙卡（复用 `mapIdx`/`lbOpenBioMap` 反向思路 L10821），不带追踪（非野生）。

### 验证
分布地图点任意黄点（含非最高级）→ 卡片含基因/属性/坐标 + 追踪按钮；点追踪 → 悬浮追踪浮标出现、可停止。

---

---

## 需求 4（新增 2026-09-05）：路线浮窗所有选择优先整体概率高的

### 背景与现象
路线浮窗（`tmp/_route_module.js`，v750+ 已含 55%/变异/词条概率模型）中多处"候选/多选一"目前按点数/覆盖数/变异数排序，未把**该方案的整体达成概率**纳入优先：
- 母池排序（达线数优先，L367/560）→ 同类母互选未按概率
- 全能公多选（变异少/出生低优先，L547）→ 多个都可当父时未比"带哪个母整体概率更高"
- 换公代高基母（fGrps，L649）→ 母按覆盖属性数，未比推进概率
- 多线合并配对（bestNew→bestCov→rare→sum，L750-771）→ 只看缺口覆盖数/稀有/点数，无"整体概率"维度
- 压垃圾洗剂、颜色配对（L900-919 both/half）等同理

### 目标
路线浮窗中**所有多选一/多候选的选择点**，统一优先"整体概率高"的方案（在其它约束如互不共用、占母排重之后作为优先/次级排序键）。

### "整体概率"口径（2026-09-05 已确认：联合全达概率）
对每个候选（公×母 / 单候选），定义**整体达成概率** = 各目标属性线在本方案下"继续推进/达成"概率的联合：
- 线已全局达顶且本配对能锁定（父母均顶/同值）→ 该线 p=1（必达）
- 线需靠本配对 55% 赌（父母不同值）→ p = 0.55（或 Robust/Frail/基因词条修正，复用 `lbRouteInhP`）
- 线为缺口（本配对低于全局顶）→ 该线不可达，p=0（未达线不完整 → 方案整体分低）
- 联合整体分 = Π(必达 1 × 各 55% 赌线 p)（与 thRow 全达口径一致）

**实施原则（用户强约束：保证不影响其他逻辑）**：
- 只作为**排序键插入**现有候选排序（同键时才比较概率），不重写候选产生/占用/展示逻辑；
- 无候选差异时（排序结果与现状相同）输出必须与现状一致（回归零变化）；
- 概率计算仅排序用，选中后的卡片/文案/note 全部沿用现有 thRow/mutSeg 输出，不另加展示。

实现：新增 `lbRouteCandProb(m, f, attrs)`（复用 mxAll/lbRoutePoint/lbRouteInhP/thRow 同源计算），作为**次/平级排序键**插入全部候选/多选排序点：
1. 母群/0/0 母排序：同达线数内按"配对后整体概率"降序（0/0 母属性相同则自然无差异）
2. 全能公多选：omnis 内先比"配当前母池的整体概率"，再比变异/出生
3. 换公代高基母：fGrps 内按推进线"整体概率"优先
4. 多线合并：bestNew（缺口覆盖）保持首位，次键由 bestCov 升级为"新增覆盖线的整体概率"（仍优先缺口线），再 rare/sum
5. **辅助决策同样概率优先（2026-09-05 用户补充：辅助也纳入）**：
   - 压垃圾洗剂多选（washCands 候选并列）→ 按配对整体概率降序取前
   - 并行种子对/叠负种子（seedPairs 排序）→ 概率键插入
   - 颜色繁育配对（cands both/half 排序 L900-919）→ 同型内按整体概率降序
   - 无多选歧义处保持现状
- 性能：仅排序时调用，候选量小；不做全局枚举。

### 验证
- 构造多候选场景（2 只全能公×不同母池、多线合并 2 组候选），核对选中顺序 = 整体概率高者优先；
- **回归**：候选无差异场景输出与 v753 完全一致（逐场景 diff）；展示文案不变。

---

## 实施顺序与版本

| 阶段 | 内容 | 版本 | 风险 |
|---|---|---|---|
| P1 | 需求 1 蛋物种还原 + 基因筛选命中 | 一版 | 低（链路单一，回归面受控）|
| P2 | 需求 3 分布地图点卡片 + 追踪 | 一版 | 低（纯前端、复用现成组件）|
| P3 | 需求 4 路线选择整体概率优先 | 一版 | 中（概率口径/排序键需确认；回归面=路线模块全部选择点）|
| P4 | 需求 2 追踪顺路 route 算法 + 持久化 | 一版 | 中（算法 + 状态，需性能计时）|

每阶段：改 `dino-import.html` → node 语法自检 → 浏览器（sim 数据/实服）验证 → bump 版本部署 → 双端回归（基因/追踪/地图/路线四入口）。

## 术语表
- 可偷蛋龙：Wyvern 火/毒/电/冰、RockDrake、Magmasaur、Deinonychus 等从巢偷蛋孵化物种
- wildEggs / 野生蛋：LB_STEAL_EGG 物种的巢蛋（source=wild/egg）
- 分布地图 lbWildDistMap：主界面按服务器展示当前物种野生龙位置的地图
- 追踪浮标 #lbTrackFloat：追踪目标悬浮指示
