# 设施汉化方案（对齐「选物种浮窗」物种翻译机制）

> 日期：2026-09-09（v2 修订）· 项目：dino-import（dino.whiterober.ccwu.cc）
> 目标：把「选物种浮窗」物种翻译的成熟机制复用到**聚落设施 chips** 中文显示。
> v2 口径修正：icon_zh_map.json 的设施表（以图标名组织）**是完整的**；真问题是**「class → 权威英文名」缺映射** + **前端没把已匹配到的图标键拿去取中文**。方案收敛为「以全服真实 160 种基名为轴的权威小表」。

---

## 一、痛点与根因（修正版）

| # | 现象 | 根因 |
|---|------|------|
| 1 | 大量设施 chips 显示英文 | 桥链断：`class(PrimalItemStructure_IndustrialForge_C…)` → 权威英文名(`Industrial Forge`) 无映射，靠正则拆前缀+camel 猜（Bed_Modern 实为 Modern Bed、ChemBench 实为 Chemistry Bench、AnvilBench=Smithy…），命中率低 |
| 2 | 中文查不到 | 前端 `zhName` 只用 toLowerCase 查键，词典 40.5% 大小写混合键（`Grill_Icon` 等）miss；且中文查询从 class 另起炉灶，**没复用「图标已匹配到的 icons2 图标名 → icon_zh_map[图标名]」**这条现成通道 |
| 3 | 词典值多义 | `icon_zh_map` 以图标为轴，值是多物品共享串（`CropPlot_Icon`=「耕地 中型耕地 大型耕地」），不能整串直显，需清洗取主名 |
| 4 | 觉得“缺词条” | 实为误解：词典设施覆盖全（经 `_Icon` 键）；缺的是**可查询的映射**，不是中文本身 |

**规模量化**：全服真实在用设施实例 13,803，**基名去重仅 160 种**（camel 即可命中 36 种，需权威英文桥 124 种）→ 一次人工校对可收敛。

**物种为什么能全覆盖**：物种有「ASB 权威物种库给英文名 → lbSpName 统一链」。设施等价物 = 一张 160 行的权威小表。

---

## 二、物种翻译机制复盘（被复用的范式）

### 2.1 数据流（lbSpName，选物种浮窗列表项 = `中文` + 灰字`英文` 同排）
```
dinoClass（唯一键，来自 ASB blueprintPath）
  └─ findSpeciesByClass → ASB_DB → 权威英文名 sp.name (eng)
       └─ 中文链 lbSpName：
          ① LB_SPECIAL_NAMES（类名→"中文 (英文)" 手动表，最高优先）
          ② zhName(dinoClass) / zhName(eng)          （icon_zh_map 小写查）
          ③ 括号次级种 LB_SUB_SPECIES、前缀剥离 lbStripPrefix、lbZhWithPrefix
          ④ lbZhDeep（v902 含 camel 拆词/去尾词逐级缩）
          ⑤ 前后缀规则（Ghost 幽灵 / X- 火山雪地 / 节日 / LB_CLS_PREFIX/SUFFIX）
  └─ 输出 {zh, eng}：UI 主行 zh + 灰小字 eng
```

### 2.2 范式要点
1. **权威英文名由数据源给**（不靠显示处猜）
2. **单一查询函数**（lbSpName）收口所有调用方
3. **可维护表分层**：手动精确表 > 通用词典 > 深度查询 > 规则拆词 > 兜底
4. **UI 同排**：中文主名 + 英文小字（用户可核对/搜索）
5. 排序/搜索均基于同一 zh（拼音 collator）

---

## 三、设施汉化方案（同机制复刻 · v2）

### 3.1 权威小表 `facility_zh.json`（以真实 160 基名为轴，一次定稿）
- 存放：服务器 `/config/www/asa-data/facility_zh.json`（随 STATIC 打包）
- 结构（三字段同源同对，图/英/中永不错位）：
```json
{
  "IndustrialForge": { "icon": "IndustrialForge_Icon", "en": "Industrial Forge", "zh": "工业熔炉" },
  "Forge":          { "icon": "Forge_Icon", "en": "Refining Forge", "zh": "精炼炉" },
  "Grill":          { "icon": "Grill_Icon", "en": "Industrial Grill", "zh": "工业烤箱" },
  "..." :            {}
}
```
- **键 = class 基名**（前端拿到 class 去前缀尾缀即查）；值三字段：
  - `icon`：icons2 图标名（前端现已能模糊匹配，图能显示 = 实证；进表固化，免每次猜）
  - `en`：权威英文显示名（依据见 §3.4）
  - `zh`：中文主名（优先 `icon_zh_map[icon键]` 的 `[ ]` 前主名；或 PO 同 ID 中文）
- **生成**（`汉化/_build_facility_zh.py` 已建雏形，升级为三字段轴）：真实 160 基名（`--fetch`）→ 图标键反查 + PO 英/中配对 → 输出候选 + `facility_zh.review.txt` 差集 → **人工一次校对**

### 3.2 前端查询函数 `lbFacilityName(cls)`（对齐 lbSpName 单一收口）
```
lbFacilityName(cls) → {zh, en, icon}
  1. 基名 = 规范前缀/尾缀剥离
  2. ① 查 facility_zh.json[基名] → 命中直接返回三字段（主路径，覆盖玩家真实在用）
  3. ② 未命中兜底：图标键反查 icon_zh_map 取主名 + zhName(en)/lbZhDeep 链（同 v902）
  4. ③ camel 拆词英文兜底（现状）
```
- `lbSettleFacMeta(cls)` 内部改调 `lbFacilityName(cls)`；图标不再单独模糊匹配（表已固化 icon）

### 3.3 chip UI 对齐物种浮窗：中文主 + 灰英文小字
- 无名项：`中文`（主）+ `<span 灰小>en</span>`；自定义名项（玩家命名，表中无、词典无）保留原名

### 3.4 权威英文依据（三层，由硬到软）
1. **同 ID 官方对**（最硬）：`ShooterGame.po`（英文，msgctxt `Content,<数字ID>`）与 `ShooterGame.json`（中文）按**同一数字 ID** 配对——这是游戏 UI 显示名本身，中英同源不可能错位（`icon_zh_map` 即此机制生成）
2. **游戏蓝图 ItemName**（绝对权威，待验证）：class→显示名的归属在游戏 `.pak` 结构蓝图 / `B:\Epic Games\ARKDevkit`；若可批量导出即全自动
3. **人工裁决**（收口）：上述查不到的 124 条，以**游戏内实际英文显示名**为准一次写死进表（用户/玩家为最终依据）

### 3.5 数据链路长期项（可选）
后端 cryo.facilities 解析时直接带 `name/en/zh`（读存档物品 ItemName）→ 前端零表零猜。

---

## 四、实施步骤与工作量

| 步 | 内容 | 量 | 谁 |
|---|------|----|----|
| 1 | `_build_facility_zh.py` 升级：160 真实基名为轴 + icons2 图标键反查 + PO 英/中配对，输出三字段候选 + review | 改脚本 | 我 |
| 2 | **校对 review（124 条候选英文/中文 + 多义清洗）** | ~120 条 | **你（一次 ~10 分钟）** |
| 3 | `_sync_repack2.py` STATIC 加 `facility_zh.json`；上传服务器 asa-data | 1 行 | 我 |
| 4 | 前端 `lbFacilityName` + `lbSettleFacMeta` 接入 + chip 中英同排 | ~80 行 | 我 |
| 5 | 版本 v903 + 语法验证 + 部署 + 浏览器核验（isl 聚落设施区） | 1 版 | 我 |

---

## 五、需要你手动做的事（唯一阻塞点）
1. **校对 `facility_zh.review.txt`**（脚本生成，预计 ~120 条）：逐条确认候选 `en`（游戏内真实英文显示名）与 `zh`（中文）；确认或改正后回写（我提供填写模板/脚本二次合并）
2. **多义词条裁决**：词典多义串（如 CropPlot 三义）你确认该设施在游戏里最准确的中文主名
3. **可选**：如你知道 Devkit/`.pak` 结构蓝图能否导出 class→ItemName，告诉我路径/工具 → 可把第 1 步变成全自动

> 其余全部由我完成：脚本升级、表生成、上传、前端接入、部署验证。

---

## 六、风险与取舍
- 词典多义词条只做种子/取主名，不整串直显
- 玩家自定义名设施保留原名（表/词典命中=系统名才翻）
- 装饰类设施（灯/椅/画/牌）已过滤不扩范围
- 表在服务器维护 → 改词条免发版

## 七、与现有系统一致性
- 图标三字段同源固化（不再每次模糊匹配）；中文经 `facility_zh` + icon_zh_map/zhName 兜底；`lbFacilityName` 单一收口可复用于容器命名/地图/统计
