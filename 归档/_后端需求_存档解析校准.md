# 后端需求：从 ASA 存档解析地图校准数据

> 发给后端（擅长解析 .ark 存档）

## 目标（第一步：先校准孤岛 Isl）
先用孤岛存档验证「存档解析 → 坐标换算校准」这条链路是否可行。孤岛已有 wiki 参考参数（mult 8000 / shift 50），后端解析结果应与之吻合；吻合后同样方法再推广到 Astraeos / Lost Colony（存档后续提供）。

| 地图 | 服务器缩写 | 存档 | 参考参数（预期） |
|------|:---:|------|------|
| The Island（孤岛） | Isl | TheIsland_WP.ark | mult=8000，shift=50 |

换算公式（ARK 标准）：`lat = Y/mult + shift`，`lon = X/mult + shift`（UE 坐标单位=厘米，地图 lat/lon 范围 0~100）。

## 需要后端提供（对孤岛存档，任选其一）

1. **世界边界/尺寸**（如有）：X/Y 方向世界范围（min/max 或边长）→ 直接得 mult、shift。
2. **已知地标 UE 坐标**：从存档提取 Obelisk（方尖碑）等固定地标对象的 `类名 + X/Y/Z`。前端已有这些地标的地图 lat/lon 可反推（每图 ≥1 个即可，2 个更准）。
3. **兜底**：存档中所有对象的 X/Y 坐标范围（min/max），用于估算。

## 输出格式（JSON 即可）
```json
{
  "map": "TheIsland",
  "worldBounds": {"minX":..., "maxX":..., "minY":..., "maxY":...},
  "landmarks": [
    {"class": "类名", "x":..., "y":..., "z":...}
  ],
  "coordRange": {"minX":..., "maxX":..., "minY":..., "maxY":...}
}
```

有世界边界给 worldBounds，没有就给 landmarks（≥2 个不同位置更好）或 coordRange。
预期：mult≈8000、shift≈50（孤岛 Red Obelisk 地图 lat 79.8/lon 17.4 ≈ UE (-260680, 238890) 可作验证锚点）。
