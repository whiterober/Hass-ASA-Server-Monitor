# 计划：蓝图显示改为卡片形式

> 状态：**设计定稿 → 待实施**（视觉方案已定稿于「二·补2」；实施前仍需确认「四、待用户决策」4 项）
> 建立：2026-09-13 ｜ 对象：`dino-import.html`（前端）+ 可能的后端导出参数
> 触发需求：**「将蓝图都显示成卡片形式」**

---

## 一、目标

容器/设施展开后，**蓝图类物品**当前以文字行呈现；改为**卡片形式**（图标 + 名称 + 数量），视觉与现有设施 chips / 物品图标风格统一，可快速扫读。

## 二、现状（已调研）

| 项 | 位置 / 结构 | 说明 |
|---|---|---|
| 物品数据 | `LB._cache.data.itemsByServer[abbr] = {byCid, byPos, byTribe, count, counts, ts}` (L3762-3776) | 来自 `item.json.gz`；`byCid[containerId] = [item…]` 逐堆明细（同资源多堆需 sum） |
| 容器查物品 | `lbContItems(...)`（L3782 附近：`byCid[containerId]`） | 容器 → 物品数组 |
| 蓝图标记 | 后端字段 `isBlueprint`（L3655：`if(d.isBlueprint===1) return;` 用于基因设备过滤） | 另有注释「**blueprint 类已按 rated=1 过滤**」(L3763) ⇒ **需先确认蓝图条目是否仍进入物品列表** |
| 书柜蓝图去重 | `lbShelfBpSet(grp)`（L8594 附近） | 本聚落书柜内的蓝图基名集合；制造设施中重复出现的同名蓝图视为「书柜远程分享」→ **不显示**（卡片化后必须保留此规则） |
| 现有卡片样式参考 | 设施 chip：`background:#1a1a24;border:1px solid #2a2a3a;border-radius:6px;padding:3px 8px;font-size:12px` + 16px 图标 + 灰英文小字 | 蓝图卡片沿用此体系，保持整站一致 |

## 二·补1 S1 数据核对结果（✅ 2026-09-13 已完成）

**结论：蓝图数据齐全，可直接卡片化，无需后端改动。**

| 项 | 实测值 |
|---|---|
| 数据源 | `LB._cache.data.itemsByServer[abbr].byCid[containerId]` ✅ 10 服均有 |
| **权威分类字段** | **`lbIsBp(it)`** ＝ `isBlueprint===1` 或 `type==='blueprint'`（**线上 v989 实际口径**）——单看 `type==='blueprint'` 只覆盖 Isl 2101 条，**会漏捧弹药/资源类蓝图**（`type='ammo'`/`'resource'`，实测漏网 890 条）；`isBlueprint=1`（Isl 3930 条）覆盖更全。另有 **`lbIsFixedRecipeBp(it)`** 排除固定配方（`rated=0` 或 `rating===-1`，实测全库 7384 条 / 117 种） |
| 全服规模 | `isBlueprint=1` 共 **11908** 条：Isl 3930 / Ext 2272 / Abe 2193 / Ast 1419 / Rag 730 / Val 393 / Cen 302 / Los 290 / Gen 197 / Sco 182 |
| 图纸特性 | **`quantity: 0`**（图纸无堆叠数量）⇒ **卡片不显示 ×N**；`stats`/`rating` 存在（可显示评分）；`kind` 可分组（weapon/saddle/armor/tek/other） |
| 物品字段全集 | `type,kind,class,key,quantity,durability,isBlueprint,rated,rating,qidx,stats,dropLoc,customClass,worldTime,containerId,containerType,containerClass,containerTribe,containerTribeName,playerName,x,y,z` |
| cid 命中规模 | Isl 598 容器 / 10068 物品；Abe 417 / 5814；Ext 322 / 4490 |

⇒ **S1 通过**：蓝图条目已在前端可查，**无需后端放开导出**；卡片判定应使用现成 **`lbIsBp(it)`**（`isBlueprint===1` 或 `type==='blueprint'`），并用 **`lbIsFixedRecipeBp(it)`** 排除固定配方（详见「三·S1」）。

## 二·补2 卡片视觉设计定稿（v1→v9 预览迭代 · ✅ 2026-09-13）

> 迭代方式：运行时注入预览面板 `#lbBpPrev`（**未改动任何源码**），用真实 10 服图纸数据渲染比对。
> 定稿原则（用户多次强调）：**1) 不改变当前其他结构 2) 只动蓝图卡片的样式结构 3) 与生物、蛋卡片样式完全一致 4) 禁止擅自改造现成样式**。

### A. 卡片 DOM 结构（定稿 · 逐行）

```html
<div class="lb-card" style="position:relative;overflow:hidden;background:#1a1a24;border:1px solid #2a2a3a;border-radius:10px;padding:10px 12px;margin-bottom:8px">
  <!-- ① 品质色带：置顶通栏（同生物卡色带结构） -->
  <div style="display:flex;height:6px;margin:-10px -12px 8px;border-radius:10px 10px 0 0;overflow:hidden"><i style="flex:1;background:{品质色}"></i></div>
  <!-- ② 水印图标（与生物/蛋卡一致） -->
  <img class="lb-card-ic" src="{lbItemIconUrl(cls)}" onerror="this.remove()">
  <!-- ③ ID 复制按钮（复用现成样式，禁止自创） -->
  <span class="lb-id-btn" onclick="event.stopPropagation();navigator.clipboard.writeText('{ID}');lbToast('已复制 ID：{ID}');return false;" title="复制ID"><i class="mdi mdi-identifier"></i></span>
  <!-- ④ 名称行：中文名 + 可选评分徽标 -->
  <div style="position:relative;z-index:1;display:flex;align-items:center;gap:8px;flex-wrap:wrap;padding-right:30px">
    <b style="color:#fff;font-size:14px">{lbItemMeta(cls).zh}</b>
    <span style="display:inline-flex;align-items:center;height:18px;background:#7c3aed33;border:1px solid #7c3aed66;border-radius:2px;padding:0 6px;font-size:10px;color:#c4b5fd;line-height:1">评分 {rating}</span>
  </div>
  <!-- ⑤ 属性行：stats 全量，禁止截断 -->
  <div style="position:relative;z-index:1;font-size:11px;color:#999;margin-top:6px;display:flex;flex-wrap:wrap;gap:4px 12px">
    <span style="color:#38bdf8">护甲 23.1%</span><span style="color:#999">耐久 6803</span><span style="color:#999">低温抗性 31.7%</span><span style="color:#999">高温抗性 17.9%</span>
  </div>
  <!-- ⑥ 材料行：占位，待配方数据源接入（见「四·4」） -->
  <div style="position:relative;z-index:1;font-size:11px;color:#f59e0b;margin-top:6px;border-top:1px dashed #2a2a3a;padding-top:6px">所需材料：<span style="color:#666">（待接配方数据源）</span></div>
</div>
```

### B. 关键样式值速查（与生物/蛋卡完全一致）

| 元素 | 类 / 位置 | 精确值 |
|---|---|---|
| 卡片外框 | `.lb-card` | `background:#1a1a24; border:1px solid #2a2a3a; border-radius:10px; padding:10px 12px; margin-bottom:8px`；必须 `position:relative; overflow:hidden` |
| 品质色带 | 卡内首元素 | `height:6px; margin:-10px -12px 8px`（负 margin 抵消卡片 padding 实现通栏）；`border-radius:10px 10px 0 0; overflow:hidden`；子元素 `flex:1; background:品质色` |
| 水印图标 | `.lb-card-ic` | `position:absolute; right:8px; top:10px; width:64px; height:64px; opacity:.14`（**禁止自绘图标容器**） |
| ID 按钮 | `.lb-id-btn` | `position:absolute; top:14px; right:8px; width:16px; height:16px; border-radius:2px; border:1px solid #444; color:#777; opacity:.6`；内含 `<i class="mdi mdi-identifier">`（**必须原样复用，禁止改造**） |
| 名称 | 内联 | `color:#fff; font-size:14px; font-weight:bold` |
| 评分徽标 | 内联 | `height:18px; background:#7c3aed33; border:1px solid #7c3aed66; border-radius:2px; padding:0 6px; font-size:10px; color:#c4b5fd; line-height:1` |
| 属性行 | 内联 | `font-size:11px; color:#999; margin-top:6px; display:flex; flex-wrap:wrap; gap:4px 12px` |
| 属性首项 | 内联 | `color:#38bdf8`（主属性高亮） |
| 材料行 | 内联 | `font-size:11px; color:#f59e0b; border-top:1px dashed #2a2a3a; padding-top:6px` |
| 卡片容器 | 外层 | 与生物卡列表容器一致（`display:flex; flex-wrap:wrap; gap:6px` 或既有 `.lb-cards-masonry`） |

### C. 品质定稿表（6 档 · 语义名称经用户确认）

| qidx | 名称（正确语义） | 游戏内英文 | 色值 | 10 服实测样例 |
|:---:|---|---|---|---|
| 0 | 普通 | Primitive | `#b8b8b8` 灰 | 暂无实例 |
| 1 | 精良 | Ramshackle | `#1eff00` 绿 | 泰克胸甲 |
| 2 | 稀有 | Apprentice | `#0070dd` 蓝 | 古巨蜥鞍 |
| 3 | 史诗 | Journeyman | `#a335ee` 紫 | 无畏巨龙平台鞍 |
| 4 | 传奇 | Mastercraft | `#ff8000` 橙 | 雪鸮鞍 |
| 5 | 神话 | Ascendant | `#00bfff` **天蓝** | 鱼叉枪 |

**依据与纠错**：
- **名称语义经用户确认（2026-09-13）**：`qidx` 0→5 依次为 **普通 / 精良 / 稀有 / 史诗 / 传奇 / 神话**。
- 曾误改为「原始 / 简陋 / 学徒 / 熟练 / 大师 / 传说」——**该写法已作废**（已从本计划与预览中删除）。
- 颜色：**最高档「神话」＝ 天蓝 `#00bfff`**；红色是**旧版 ARK: Survival Evolved（ASE）** 的最高档配色，ASA 已改为天蓝（用户确认）。
- 曾误列 **7 档**（多出 Tek `#00ffff`）**已删除**，**共 6 档**。
- 判定字段：`item.qidx`（0-5）；越界 / 无值时按 **普通档**兜底。

### D. 迭代反馈 → 定稿规则（逐条留档）

| # | 用户反馈要点 | 定稿规则 |
|:--:|---|---|
| 1 | 无需显示「图纸」字样 | 卡片**不出现"图纸 / Blueprint"文字标签** |
| 2 | 卡片名称必须对应中文 | 名称一律 `lbItemMeta(cls).zh`（禁止显示英文或原始 class） |
| 3 | 右上角 ID 也要有，功能要全（点击复制） | 必须带 ID 复制按钮：`navigator.clipboard.writeText(ID)` + `lbToast('已复制 ID：…')` |
| 4 | 容器不落坐标都无需显示 | 卡片**不显示坐标**（x / y / z） |
| 5 | 最关键的蓝图属性全都没显示 | **stats 全字段展示**，禁止只显示部分或省略 |
| 6 | 图标没按生物、蛋卡片样式 | 图标必须用 `.lb-card-ic`（absolute 水印 64×64 / opacity .14），源 `lbItemIconUrl()` |
| 7 | 所有属性都要显示；**所需材料非常重要** | 属性全量 + **新增材料行**（配方数据源暂缺，先占位，见「四·4」） |
| 8 | 「已复制」以及 ID 被泄露错误显示 | 禁止常显 ID 文本；复制反馈只能走 `lbToast` 临时提示 |
| 9 | 为何擅自改样式？生物卡片的 ID 按钮不是这样的 | ID 按钮**必须原样复用** `.lb-id-btn`，**禁止自创样式** |
| 10 | 品质应用游戏内颜色，和生物一样放到顶部一条 | 品质用**顶部 6px 通栏色带**（结构与生物卡色带一致） |
| 11 | 每种品质都给我个样例 | 预览需**逐档给真实样例**；无数据的档位显式标注"暂无" |
| 12 | 传说应是天蓝色；中文说法不对，去社区找正确的 | 见「C. 品质定稿表」——**6 档语义名称 普通 / 精良 / 稀有 / 史诗 / 传奇 / 神话**（用户确认）；**最高档「神话」＝ 天蓝 `#00bfff`** |

### E. 与生物 / 蛋卡片的复用清单（S2 实现硬约束）

| 复用项 | 现成来源 | 说明 |
|---|---|---|
| 卡片外壳 | `.lb-card` | 蓝图卡与生物卡**同一套**圆角 / 描边 / 底色 / 内边距 |
| 色带 | 生物卡顶部色带结构 | 生物卡用物种品质色，蓝图卡用图纸品质色 |
| 水印图标 | `.lb-card-ic` + `lbItemIconUrl()` | 蓝图与生物同为 64×64 水印；**不新增图标类** |
| ID 按钮 | `.lb-id-btn` + `mdi mdi-identifier` | 蓝图与生物同一按钮；**禁止新增按钮样式** |
| 中文名 | `lbItemMeta(cls).zh` | 与物品/生物命名口径一致 |
| 属性数值 | 现有 ÷100 百分比口径 | 百分比类属性（护甲/伤害）显示为 `xx.x%`，绝对值（耐久）显示整数 |

## 三、实施步骤（分步可验证）

| 步骤 | 内容 | 涉及文件 | 验证方式 |
|---|---|---|---|
| **S1** | **数据核对**：取一个已知含蓝图的聚落/容器，用 `page.evaluate` 检查 `itemsByServer.byCid[cid]` 中是否存在蓝图条目；若被后端过滤 ⇒ 与用户确认是否需要后端放开导出（**后端改动须单独确认**） | 只读排查 | ✅ **已完成**：判定用现成 **`lbIsBp(it)`**＝`isBlueprint===1` 或 `type==='blueprint'`（**只判 `type` 会漏 890 条**：弹药/资源类蓝图 `type` 为 `'ammo'`/`'resource'`）；固定配方用 `lbIsFixedRecipeBp(it)` 排除 |
| **S2** | 新增 `lbItemCard(item, opts)`：**严格按「二·补2 · A/B」定稿结构实现**（`.lb-card` 外壳 + 品质色带 + `.lb-card-ic` 水印图标 + `.lb-id-btn` 复制 + 中文名 `lbItemMeta().zh` + 评分徽标 + **stats 全量属性行** + 材料行占位）；**不显示数量（`quantity=0`）、不显示坐标、不显示"图纸"字样、不出现 ID 常显文本**；颜色/类名**全部复用**生物卡现成样式，禁止自创 | `dino-import.html` | 与生物卡渲染样例逐项比对 |
| **S3** | 容器物品区改为 `display:flex;flex-wrap:wrap;gap:6px` 的卡片容器；**蓝图单独分组**（如标题「📘 蓝图」）置顶或末尾（见「四·3」） | `dino-import.html` | 浏览器渲染核对 |
| **S4** | 性能与分批：大容器（Ext 常 150+ 条）沿用现有 `CH=150` 分批渲染模式，避免一次性 DOM 膨胀；卡片 DOM 复用字符串拼接（勿引入逐条 DOM 操作） | `dino-import.html` | 展开大容器时观察卡顿 |
| **S5** | 部署 CF + 三端验证（PC / 手机 / PWA）：卡片渲染正确、图标不缺失、书柜去重仍生效、滚动流畅 | `tmp/_deploy_cf.py` | 线上 `fetch` 版本 + 浏览器实测；SW 缓存需递增时同步 `pwa/sw.js` VER |

## 四、待用户决策（实施前需 4 项确认）

1. **卡片尺寸**：① 小 pill（与设施 chip 同高，一行可放 8-10 个）；② 中卡（约 96px 宽，图标在上、名字在下，可放 4-5 个）；③ 大卡（120px，含中文+英文+数量）
2. **范围**：仅**蓝图**卡片化，还是**容器内全部物品**统一卡片化（推荐统一，避免两套视觉）
3. **分组**：蓝图是否**单独分组**（如「📘 蓝图 ×N」标题 + 独立卡片区）还是与普通物品混排
4. **材料数据源**（新增 · 2026-09-13）：用户强调「所需材料非常重要」，但当前数据链**完全无配方数据**——
   - 已核实：原始导出 `{abbr}_item.json.gz` 物品对象仅 22 字段，**无 `cost`/`craft`/`recipe`/`material`/`ingredient`**；后端 `dino_backend.py` 仅转发；前端 `LB_ITEM_ZH`（510 条）与 `ASA-values.json` 亦无配方。
   - 方案 A（推荐）：**离线 ARK 配方 JSON 挂 CDN**，前端按 `class` 查表合成材料行（零后端改动、零插件改动）。
   - 方案 B：**后端 / 插件导出扩展**（新增配方字段），改动面大、需重导存档。
   - 未定期间：卡片材料行保持占位「（待接配方数据源）」，**不阻塞 S2 其余部分实施**。

## 五、风险与规避

| 风险 | 规避 |
|---|---|
| 蓝图被后端过滤（`rated=1`）⇒ 拿不到数据 | ✅ 已排除：S1 实测 `type='blueprint'` 条目均可在前端查到（见「二·补1」） |
| 书柜远程分享去重（`lbShelfBpSet`）失效 | S2/S3 调用点保留 `_shelfBp` 过滤，卡片化只改呈现不改筛选 |
| 图标缺失显示空白 | 定稿口径：`.lb-card-ic` + `lbItemIconUrl(cls)` + `onerror="this.remove()"`（与生物卡一致；**不再用 mdi-cog 占位**） |
| 大容器渲染卡顿 | 沿用分批（150/批）+ 字符串拼接，禁止逐条 append |
| 移动端横排溢出 | 卡片容器 `flex-wrap` + 字号 11-12px，与现有 chips 一致 |

## 六、验收标准

- 蓝图以**卡片**呈现（品质色带 + 水印图标 + 中文名 + 评分 + 属性 + 材料行），不再是一行文字；
- 视觉与**生物 / 蛋卡片**完全同一套语言（圆角、描边、底色、色带、水印图标、ID 按钮），**不改动其他既有结构**；
- **品质色带**按 6 档配色（普通灰 / 精良绿 / 稀有蓝 / 史诗紫 / 传奇橙 / **神话天蓝**）；
- **ID 复制按钮**功能可用（点击复制 + toast 提示），且不出现 ID 常显泄露；
- **属性全量展示**（stats 全字段，含护甲 / 耐久 / 低温抗性 / 高温抗性 或 伤害 / 耐久）；
- **不显示坐标**、**不显示"图纸"字样**；
- 书柜蓝图去重规则**仍然生效**；
- PC/手机/PWA 三端渲染一致，展开大容器无明显卡顿；
- 线上版本号可查、有回滚备份（`bak/dino-import_backup_*.html`）。

---

> 📌 下一步：S1 数据核对 **已完成**（见「二·补1」）、卡片视觉 **已定稿**（见「二·补2」）；待用户确认「卡片尺寸 / 卡片化范围 / 是否分组 / 材料数据源」**四项**后，从 **S2 实现 `lbItemCard()`** 开始编码实施。
