# 路线浮窗修正待应用记录（模拟→记录→统一应用）

> 工作流（2026-09-04 用户设定）：用户提修正 → 本 Agent **直接浏览器模拟渲染**展示效果（不部署生产）→ 记录到本文档 → 等用户通知「统一应用」后一次性 patch + 部署。

## 当前线上基线
- 版本：`v20260904-713`（2026-09-04 全量统一应用 17 项后上线）
- 已上线内容：v712…v713c（旧基线）→ 2026-09-04 统一应用 v714–v721j 共 17 项
- 模拟渲染基线 = 本地 `tmp/_route_module.js`（= 线上 713 内容 + v722/v723 待应用）

## ✅ 2026-09-04 已统一应用（线上 v20260904-713，17 项全部上线）
> v714 扩母0/0×0/0 · v715 主配优先 · v716/717 并行行距 · v718 扩母标题 · v719 徽章统一 · v720 全能公模型 · v721a 命名 · v721b 换代单卡 · v721c2 母覆盖+理论行 · v721d 负线理论行 · v721e 换公代 · v721f 隐藏叠空卡+缺口可执行 · v721g 同父多母单行 · v721h 理论行三态 · v721i 叠变异（全能公） · v721j 叠负变异
> 下方 v714–v721j 各条目均已部署上线；**待应用清单现仅剩 v722 / v723**（本次统一应用后新增，待下次通知）

## 修正待应用清单
<!-- v714–v721j 17 项已应用上线（v20260904-713）；v722/v723 为新增待应用 -->
### 2026-09-04 · v714：扩母机制修正（0/0 只能由 0/0×0/0 产出）
- **问题**：扩母卡原用「变异线父（计数≠0）× 低计数可续母」繁育，但 A5 子代计数 = 父母同侧计数相加 → 带计数亲本必传计数 → 永远产不出 0/0 子代
- **修正**：扩母繁育源改为「空闲 0/0 种公（zeroMales 中未占用）× 现有 0/0♀（breedF，可复繁不消耗）」；无 0/0 种公可用（线父均已带计数）时降级说明「只能外部 抓/孵/交换 0/0♀」；删除死代码 pickFree/adF
- **本地模块**：`tmp/_route_module.js` 已改（v714），未部署
- **模拟渲染**（浏览器 toString 打补丁注入，未部署）：✅ A 有 0/0 种公 → 扩母群 `耐种公 × 零血(达线) × 零血2` + 理论「0/0×0/0 → 子代计数 0/0 可入群」；✅ B 无 0/0 种公 → 降级「当前无 0/0 种公 → 只能外部 抓/孵/交换 0/0♀」

### 2026-09-04 · v715：组排序 主配永远在并行前
- **问题**：压垃圾(tag=并行, order1) 排在叠攻(tag=主配, order2) 前，主配被压后
- **修正**：排序键改 `_grk(g)`（主配=0/并行=1/可选=2）→ `(_grk差)||(ai差)||(order差)`，主配组全局前置
- **本地模块**：已改（v715），未部署
- **固定场景套模拟**（同一套 10 场景，v715 注入后 __renderSimAll）：✅ 场景④ 组序 = 主配叠攻变异 → 并行压垃圾 → 并行扩母群 → 并行叠血 → …（压垃圾已在主配后）

### 2026-09-04 · v716：内部并行行距收紧
- **问题**：负线/多线 内部并行行间 margin-top 6px + padding 8px，行距偏大
- **修正**：两处（负线行、多线行）`margin-top 6→3`、`padding 8px→3px`（保留虚线分隔）
- **本地模块**：已改（v716），未部署
- **固定场景套模拟**：✅ 行距收紧生效（两处 gi?3:0/3px 0 0）

### 2026-09-04 · v717：内部并行之间去除分隔线
- **修正**：负线/多线 内部并行行移除 `border-top` 虚线分隔线与顶部 padding，仅保留 3px 行距
- **本地模块**：已改（v717），未部署
- **固定场景套模拟**：✅ 两处无分隔线（border-top:1px dashed 清零）

### 2026-09-04 · v718：扩母卡标题带目标属性
- **问题**：场景② 同时出「扩母群 3/10」与「扩母群 0/5」两条易混淆；用户确认保持各属性各一条但需标明为哪属性扩
- **修正**：标题 `扩母群` → `扩+属性名+母群`（如 `扩攻母群 · 0/0母 3/10`、`扩血母群 · 0/0母 0/5`）
- **本地模块**：已改（v718），未部署
- **固定场景套模拟**：✅ 场景② = 扩攻母群 4/10 / 扩血母群 0/5（冒烟断言同步改「母群+0/0母」）

### 2026-09-04 · v719：并行徽章统一函数
- **验证**：样式字符串一致，唯一差异=多线合并内联徽章尾部无空格（paraBadge/颜色有空格）→ 间距细微不一致
- **修正**：多线合并徽章改调统一 `paraBadge(gi)`（颜色部位在不同函数作用域，样式已一致不改）
- **本地模块**：已改（v719），未部署；冒烟全绿

### 2026-09-04 · v720：叠变异改全能公模型 + 0/0 母公用池（用户确认 A）
- **背景（用户分析结论）**：单父多属性——一个公把所选属性全堆最高，×母群配一窝，孵出看变异落点归各属性线；母群天然公用（不存在多父抢母）
- **修正**：①叠组由「每属性各父」改为「全能公」驱动——仅当存在 ♂ 同时所有选中属性=全局最高时出一张 `叠攻血重变异（全能公）`（父×公用母群、note 落点分流/父到20预警）；无全能公 → `需先造全能公` 提示（列各属性最高缺口）②0/0 母公用池不分属性，扩母单条 `公用母群 · 0/0母 X/N`（N=lbRouteMomGet，全能公到20→2N）置主配区最前（order-1）
- **本地模块**：已改（v720），未部署；冒烟断言适配（叠攻血重变异（全能公））后全绿
- **固定场景套模拟**：✅ ①-④ 多属性无全能公→`需先造全能公`+公用母群；⑥⑦⑧⑨ 单属性全能→叠组卡+公用母群；⑤ 公用母群扩产；⑩ 公用母群降级外部

### 2026-09-04 · v721a：命名统一
- ①`公用母群`→`扩母群`（公用池语义保留在 note）②`叠全能变异（需先造全能公）`→`叠变异`
- 本地已改（v721a），未部署；固定套模拟 ✅

### 2026-09-04 · v721b：换代合并单卡（用户确认：造父期与全能期同逻辑；深负公完全并入）
- **验证结论**：换代/造父本质同一动作（目标♂某属性基础 < 0/0 可及上限 → 高基母 × 目标♂ → 新公保留变异+提基础）；深负公仅是起点♂特殊
- **修正**：替换原 per-属性 换代/补♂ 段为单卡——目标♂=omni（全能公续提）｜合计最高♂（打造全能公）｜hasNegM→最负深负公（深负公续提）；每属性行「目标♂×0/0 高基 → 基础 a→b」或缺失行（需产公/并入/抓）
- **本地模块**：已改（v721b），未部署；冒烟全绿
- **固定场景套模拟**：✅ 每场景 ≤1 张（①深负公续提 / ②③④打造全能公 / ⑥-⑩已顶或无需），旧 per-attr 换代清零

### 2026-09-04 · v721c→v721c2：换代单卡串行逻辑 + 补回理论行（用户反馈：两条同父不能并行 + 丢了理论）
- **用户反馈**：①换代组应与其他组一致串行逻辑、内部不可伪并行；②其他组都有灰字理论行（叠组 thRow/多线合并/压垃圾/扩母），唯独 v721b 重写后丢了
- **v721c（初版，已弃）**：按属性拆 step1→step2 串行链 + 每步理论行 → 模拟发现固定套①的「高基母」实为**双高母（攻48·血48 0/0）**，拆两步是错误拆分（理论一步血即达 48）
- **v721c2（最终，本地已实施）**：换代改为**按 0/0 母覆盖贪心**——母能覆盖全部待提属性 → **单步**（`父×双高母 → 攻基础44→48 · 血基础42→48` + 理论行 `理论 攻48↑·血48↑（55%×2：单只≈30.3%·平均≈3只出1只全达）`，与叠组/多线合并完全同构）；只能分开覆盖 → 才串行步骤链（每步父=上一步新公）；理论行按**基础口径**（max 父累积基础/母基础），↑=本步提升、达全局顶绿；缺口♂行保留附后
- **本地模块**：已改（v721c2），未部署；node --check RC:0；冒烟全绿
- **固定场景套模拟**：✅ 每场景换代 ≤1 卡、无步号冗余（①深负公续提=单步双提+理论行；②③④打造全能公；⑥-⑩已顶或无需）；理论行形态与其他组统一
- **组内理论行审计**：叠组/扩母/压垃圾/多线合并 ✅ 均有理论行；**负线（负计数溢出）行无灰字理论行**（唯一例外，机制说明型）→ 待用户定夺是否补

### 2026-09-04 · v721d：负线行补理论行（用户确认「补简短理论行」）
- **修正**：负计数溢出线每行（种子对）下补灰字理论行：`理论 计数 1/0 起点（约 4–5 蛋 1 次变异）→ 同源互配翻倍 1+1=2→4→8… → int32 溢出变负`，与其他组视觉统一
- **本地模块**：已改（v721d），未部署；node --check RC:0；冒烟全绿
- **固定场景套模拟**：✅ ⑤负线每对种子行带理论行（2 行 = 2 对，与并行徽章数一致）

### 2026-09-04 · v721e：命名「换代」→「换公代」
- **修正**：用户可见文案 4 处改名（代码注释保留历史标识）：①卡 title `换代（…）`→`换公代（…）` ②allTop 补齐列表（换代/叠/压缩）→（换公代/叠/压缩）③note「高基母换代：」→「高基母换公代：」④note「深负公换代后仍深负」→「深负公换公代后仍深负」
- **本地模块**：已改（v721e），未部署；node --check RC:0；冒烟全绿
- **固定场景套模拟**：✅ 5 张卡标题全为「换公代」（深负续提 1 + 打造全能 4），无用户可见「换代」残留（仅固定套场景描述名含「换代」）

### 2026-09-04 · v721f：无法叠时隐藏叠组 + 缺口行转可执行配对（用户反馈 + A 方案确认）
- **用户反馈**：①无法叠变异时「叠变异」空提示卡应隐藏 ②附件缺口行「· 血：另有 ♂ 最高 46 点需并入目标♂」不该只给文字——缺属性（基础 0）但库里 0/0 高基♀ 可引入 → 应直接给配对开始做
- **修正**：①删除 `else if(allTop)` 空提示卡（无 omni 时不 push 叠变异组）②换公代 needKs 放宽：mBase=0 但有 0/0♀ 基础更高 → 纳入可执行 step（`目标♂ × 0/0母 → 血基础 0→46` + 理论行）；仅无 0/0♀ 才保留「抓/孵/交换」提示 ③全场无正基础点（纯 0/0 造负场，如⑤）→ 整张换公代卡隐藏（anyPos 守卫）
- **本地模块**：已改（v721f），未部署；node --check RC:0；冒烟全绿
- **固定场景套模拟**：✅ ①-④ 叠变异空卡消失；②③④ 打造全能公=可执行 step（② 血父×零A→攻0→45；③ 攻次男×血母零→血0→46 理论 攻46·血46↑；④ 两步链引入血/重）；⑤ 换公代整卡隐藏；⑥-⑩ 真叠组保留

### 2026-09-04 · v721g：换公代多属性改「同父多母单行」不拆步（用户确认 B 方案）
- **用户反馈**：换公代（打造全能公）多属性时我拆成「步1/步2」接力链（④：步1 攻父×血母零3→血0→46；步2 新公1×重母零3→重0→47）——没对齐其他组（负线/多线合并/压垃圾用「并行 N」徽章或单行，且靠理论行表达逐步逼近）；用户 freeText「对齐其他组逻辑，关键是 逐步逼近最终目标」
- **机制确认**：子代每属性只从单只母 55% 取高 → 单只仔无法同时拿两只不同属性母的改进 → 多属性只能分窝推进（同父可分别配多母），无法虚假并行
- **修正（v721g）**：删除 步1/步2/新公N 伪父接力链 → 同父多母单行（父 × 母A(血46·0/0高基) × 母B(重47·0/0高基) 一行，无步号/并行徽章）+ `→ 血基础0→46 · 重基础0→47` + 理论行按母分窝（`血46↑（55%）· 重47↑（55%）`；单母覆盖多属性则 55%×N）+ note「分窝推进·逐步逼近」；与叠组（父×公用母群）视觉对齐
- **本地模块**：已改（v721g），未部署；node --check RC:0；冒烟全绿
- **固定场景套模拟**：✅ ①深负公×双高母单行（55%×2）；②③单母单行；④ 攻父×血母零3×重母零3 同父多母单行（无步号、理论分窝）

### 2026-09-04 · v721h：理论行属性视觉三态统一（用户问：为何有的粗绿/有的白细/有的灰）
- **用户疑问**：理论行里属性视觉不统一——有的粗体绿（thRow/多线合并达顶用 `<b>`）、有的白细带↑（v721g 换公代）、有的灰细（thRow 未达继承 #888）→ 原因：两套代码时期产物
- **A 方案确认三态规范（全模块统一）**：①达全局顶=粗体绿 `<b style="color:#4ade80">` ②本步推进未达顶=细体白 #e6e6ef + 青↑ ③保底/非目标=细体灰 #999
- **修正（v721h）**：thRow else + 多线合并 else 未达顶 → 显式灰 span(#999)；换公代 per 达顶 → `<b>` 粗绿（去掉↑）、未达 → 白+↑
- **本地模块**：已改（v721h），未部署；node --check RC:0；冒烟全绿
- **固定场景套模拟**：✅ ③ 血46=达全局顶 → 粗绿无↑；④ 血46/重47 < 顶女48 → 白细+↑

### 2026-09-04 · v721i：全能公叠组标题去属性名 →「叠变异（全能公）」（用户确认）
- **用户反馈**：看到「叠攻变异（全能公）」困惑——v721a 不是已统一成「叠变异」？先澄清卡片体系：能叠时的推进卡（父×公用母群）v720 命名「叠N变异（全能公）」；v721a 改的是无全能公时的提示卡「叠变异」（v721f 已删）。用户确认推进卡也去属性名
- **修正**：title `'叠'+tK+'变异（全能公）'` → `'叠变异（全能公）'`（tK 仍用于 note 说明变异落点属性）；冒烟断言同步（叠攻血重变异（全能公）→ 叠变异（全能公））
- **本地模块**：已改（v721i），未部署；node --check RC:0；冒烟全绿（断言同步后）
- **固定场景套模拟**：✅ 5 张全能公叠组卡标题全为「叠变异（全能公）」，无属性名残留；提示卡仍隐藏

### 2026-09-04 · v721j：负线标题「负计数溢出」→「叠负变异」
- **修正**：负计数溢出线卡 title `负计数溢出` → `叠负变异`（与叠变异/换公代命名风格统一）；冒烟断言「负计数溢出线<0」不受影响
- **本地模块**：已改（v721j），未部署；node --check RC:0；冒烟全绿
- **固定场景套模拟**：✅ ⑤ 负线卡标题为「叠负变异」，无「负计数溢出」残留

### 2026-09-04 · v722：浮窗标题栏属性徽章 序号→库存最优 基础|变异 点
- **用户反馈**：标题栏 `血① · 重② · 攻③` 序号区，希望显示当前库存内该属性最优的 基础|变异 点
- **修正**：lbRouteRender 标题徽章 `属性+序号` → `属性 + 全场该属性 total 最高龙的 lbRoutePtDisp(基础|变异)`；删 zhNum 死代码
- **本地模块**：已改（v722），未部署（本次 17 项统一应用不含此条）；node --check RC:0
- **模拟验证**：✅ 标题显示 `血 42|2 · 重 42|0 · 攻 44`（基础|变异、无序号）

### 2026-09-04 · v723：Xn|n 基础值染属性标准色（A 方案确认：全局统一）
- **用户反馈**：浮窗内所有 `Xn|n`（基础|变异）前面的基础值应染该属性对应颜色（变异保持橙）——现基础无色继承灰/白
- **A 方案**：全局属性标准色表 `LB_ROUTE_ATTR_COLS=['#22c55e'血绿,'#a78bfa'重紫,'#fbbf24'攻黄,'#60a5fa','#f472b6','#4ade80']`（按 attrs 序前 6 色）；lbRoutePtDisp 基础染 `LB._routeAttrCol[k]`（lbRouteRender/lbRoutePlanHtml 开头按 attrs 建映射）
- **本地模块**：已改（v723），未部署；node --check RC:0；冒烟全绿
- **模拟验证**：✅ 43/43 个 `基础|变异` 全部染色（血42→#22c55e、重42→#a78bfa、攻44→#fbbf24），变异保持橙 #f59e0b

### 2026-09-04 · v732：变异概率口径 = 社区标准算法 7.6%（待批次上线）
> ⚠️ 说明：记录文档基线滞后——本会话 v722–v731 已按「改完直接上线」逐一部署（线上现为 `v20260904-721`）；本条 v732 为**纯口径常量落地 + 记录**，用户选择「仅改口径暂不上线」，待下次批次统一上线。
- **用户决策（2026-09-04）**：网上社区算法 7.6% 为准，**替换**项目内旧经验值「约 4–5 蛋 1 次变异」（代码 note / 负线目标行 / 叠组文案均受影响，实施时统一替换）
- **社区口径**：每侧（父系/母系）每蛋最多 3 次 roll，单次成功 2.6% → 单侧至少 1 次 = 1−(1−0.026)³ ≈ **7.6%**；双侧（父+母都可用时）至少 1 次 ≈ **14.6%**；落点属性随机、池 11（单属性命中 1/11，对齐报告 L162）
- **本地模块**：已加可调常量（`LB_ROUTE_MUT_ROLLS=3` / `LB_ROUTE_MUT_P=0.026` / `LB_ROUTE_MUT_ATTRS=11` / `LB_ROUTE_MUT_SIDE≈7.6%` / `LB_ROUTE_MUT_ANY≈14.6%`，顶部 LMAX 常量区后）；node --check RC:0；**未部署**
- **待实施（方案 A，已与用户确认参数 3·2.6%·11）**：叠变异/主配/换公代组卡 note 动态显示「变异期望」——父本总<20：父系 ~7.6%/蛋 × 落点 n/11 → 约 N 蛋 1 只目标+1；父本总≥20：父系锁 0（仅母系）≈有效减半 + 母群×2 提示；父本负：全开；并把旧「4–5 蛋 1 次」文案统一替换
- **校准建议**：可用 `\\SERVER\ARK Server\DinoData` 全服蛋谱系实测本服实际变异率，校验 7.6% 是否匹配（服务器可调 mutation 倍率）
