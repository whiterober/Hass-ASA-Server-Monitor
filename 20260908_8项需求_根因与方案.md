# dino-import 8 项需求 · 根因与方案分析（2026-09-08）

> 版本基线：`dino-import.html` v876（v20260907-876）。本文档只做根因分析 + 实施方案设计，不含代码改动。实施时新版本号建议递增至 v877+。

---

## ① 路线浮窗：交配倒计时手动刷新（低可视化图标）

### 现状
- 前端 rec 已携带 `nextAllowedMatingTime`（世界秒，tamed 源，L3239）+ 顶层 `worldSecondsNow`（`worldNowByServer`，L3349，tamed 源更新鲜则覆盖）。
- `lbRouteMateTag(r)`（L12943）：`nm = nextAllowedMatingTime`、`wn = LB._cache.data.worldNowByServer[...]` → 冷却>0 显示 `🕒Xh/Xm`，冷却=0 显示可配。
- 该值来自**一次性的文件导入缓存**（`LB._cache.data`），导入后不再变化；母龙冷却在游戏中随时间推进，浮窗不会自己更新。

### 根因
倒计时是**快照**：世界时间推进后，`wn` 与 `nm` 的差值不会自动变化 → 显示停留或过期，无刷新入口。

### 方案
后端已确认支持（ArkGetDino 纯追加输出 `worldSecondsNow` + `nextAllowedMatingTime`，`CMD_GET_DINO = 'TransferIdentityFix.ArkGetDino'`，`dino_backend.py` L122 / get_dino handler L779）：
1. **后端**：确认 get_dino 返回 JSON 已含 `worldSecondsNow`/`nextAllowedMatingTime`（若未透传则 extra 追加，纯追加不破坏旧字段）。
2. **前端新增轻量接口**：`lbQueryMating(server, dinoId)` → POST ArkGetDino → 只回写该 rec 的 `nextAllowedMatingTime` + 更新 `worldNowByServer[server]` → 不整表重渲染。
3. **低可视化 UI**：倒计时母卡右侧放 12px `mdi-refresh` 淡灰小图标（opacity .45，hover 提到 .9），点击即刷新该卡倒计时；不新增显眼按钮、不进操作日志。
4. 复用既有 `lbQueryDinoStatus` 连接/错误处理模式（关注未成年浮窗周期轮询同一 RCON 链路），失败静默降级（保留旧值）。

---

## ② 按当前服交配 CD 计算最佳母群数量

### 现状
- `lbRouteMomGet()`：母群档位预设 3/6/12/24/48，推荐值按"现有 0/0 母可用数"给出。
- 未考虑该服实际交配冷却：母群数量与"多久能产一窝"直接相关。

### 已知参数
- 本服 `MatingIntervalMultiplier = 0.25`。
- 官方全物种交配 CD 基础：**18~48 小时随机** → ×0.25 = **实际 CD 4.5~12 小时**，均值 ≈ **8.25 h**。

### 根因
母群档位是固定猜测值，未与"期望产出节奏 × 实际 CD"挂钩。N 只可配母的平均产蛋间隔 ≈ `平均CD / N`（母可并行冷却，CD 主导）。

### 方案（单标准：现实 24h 出 1 个有效变异 → 替换末位 48 档）

> 用户修正（2026-09-08）：不要 3 档/多建议值；单一标准 = **现实时间 24 小时能产生 1 个有效变异**，并把算出的推荐值**替换掉每个物种母群档位末位的 48**。

推导：
- 本服实际交配 CD：官方基础 18~48h 随机 → ×`MatingIntervalMultiplier`(0.25) = 实际 **4.5~12h**，均值 CD_avg ≈ **8.25h**。
- N 只可配母并行：平均每 `CD_avg / N` 小时产 1 颗受精蛋（母各自独立冷却）。
- 设每蛋产出**有效变异**的概率 P_eff（方向口径 = 攻/血单向或任一可用需用户校准；变异上限 ≥20/20 后概率衰减需一并折算）。
- 目标 24h 得 1 个有效变异 → 24h 内需产 `1/P_eff` 颗蛋 → 母群数：

$$
N_{opt} = \lceil \frac{CD_{avg} \times (1/P_{eff})}{24h} \rceil = \lceil \frac{8.25}{24 \times P_{eff}} \rceil
$$

- 默认 P_eff 示例（**显式可校准参数**）：
  - P_eff = 3% → N_opt ≈ **12**
  - P_eff = 5% → N_opt ≈ **7**

落地（用户指定）：
- 每物种档位列表末位 `48` → **替换为该物种按公式算出的 N_opt**（即 `LB_ROUTE_MOM_PRESETS` 变 `[3,6,12,24,N_opt]`，N_opt 为最高档与默认推荐，按物种持久化）。
- 去掉原多档建议与 T 目标间隔三值，只保留单一推荐值。
- P_eff 作为可调参数（按物种或全局），玩家可随实际变异产出率校准；缺 0/0 母时衔接既有"需补 N 只 0/0 母"提示（受库存可配 0/0 母数与变异上限约束）。

---

## ③ Gen 追踪功能未考虑分海图（与地图标点不一致）

### 现状
- 地图标点已按 **z 中位数**分海/陆图：`lbMapForServer(abbr, zmed)`（L2164 附近，`oceanImg`/`oceanZ=200000`/`oceanMultLat/Lon`/`oceanShiftLat/Lon` 海图独立锚点），聚落标注（L11726）正确按海图标。
- 追踪链：`lbTrackNext()`（L10543）→ `lbNearestWild(pos, excludeId)`（L10081）→ 地图浮窗绘制。

### 根因
追踪目标（野生龙坐标）与聚落标点不同：追踪用 `lbMapForServer` 取图时若按 **z**（目标龙所在高度）判断海/陆，而筛选/候选用的是主图（陆图）坐标换算，或直接用 `m.img`（陆图）未走 ocean 分支 → 海里的目标龙被画到陆图错误位置（与标点规则不一致）。

### 方案
1. 追踪绘制/候选坐标换算统一走 `lbMapForServer(abbr, zTarget)`：目标龙 `z >= oceanZ` → 海图（用 `oceanMultLat/oceanMultLon` 换算）；否则陆图。与聚落标点同规则。
2. 追踪浮窗"海图/陆图"切换与地图标点一致：进入追踪时若目标在海图，自动切海图（复用 map 绘制函数而非单独画布分支）。
3. 验证：Gen 服海龙（安康鱼/水母类深水目标）追踪定位与标点叠加一致性。

---

## ④ 废种判定需考虑颜色稀缺度

### 现状
- `lbRouteWasteList(recs, attrs)`（L13901）：废种按配种价值判定（非 0/0、无有效属性、无可留颜色组合等），每只标理由。
- 颜色维度：`lbRouteColorsArr(r)`（L12862）/ `lbRouteColorHtml`（L13784）已有颜色区渲染，但**不参与废种判定**。

### 根因
判废只看属性/留种，携带稀有色（如特定 event 色、高编号区域低概率色）的龙即使属性无价值也被判废 → 玩家丢龙丢色。

### 方案（用户口径：全库存唯一）

> 用户修正（2026-09-08）：稀缺颜色 = **该颜色 ID 在当前玩家全库存中只有这一只龙携带**（出现次数 = 1）。

1. **稀缺判定**：对每只候选龙，遍历其颜色区所有颜色 ID，统计该 ID 在当前玩家全库存（全部已导入 tamed rec，含蛋/胚胎外的所有携带者）出现次数；**任一颜色区 ID 全库存计数 = 1** → 该龙判定为「携带稀缺色」。（口径注：用户主口径=全库存；若后续需要可另加"同物种内唯一"第二口径，默认不启用。）
2. 判废规则插入：`wastes` 前先做颜色检查——携带 ≥1 全库存唯一色 → 降级为「⚠ 颜色稀缺·建议保留」单独分组，不进废种批量丢弃区。
3. 废种卡 title/理由追加颜色稀缺标注（`lbRouteColorHtml` 已有色块渲染，可复用缩略版）。
4. 交互补充：废种确认弹窗若含"颜色稀缺"项，额外二次确认文案提醒。

---

## ⑤ 生物卡片标全库存最优基础/变异属性的特殊颜色

### 现状
- `lbDinoHTML(r, opts)`（L4267）属性行渲染：血/耐/氧/食/重/攻，基础与变异数字各有既有染色（v376 血/食/重当前值逻辑等）。
- 路线浮窗已有 `lbRouteBestMy` 类全局最优计算（属性线推荐），但**普通生物卡片**不标"是否全库存最优"。

### 根因
卡片只显示单龙绝对值，玩家需自行记忆/对比才知道哪只是全库存该属性最高——选种/清仓时低效。

### 方案（性能结论 + 无角标仅数字变色加粗）

> 用户修正（2026-09-08）：评估性能影响；**不要角标**，只把命中全库存最优的数字做特殊颜色 + 加粗。

**性能评估（结论：影响极小）**：
- 若每张卡渲染时全量扫池 → O(卡数 × N) 最坏约数十万次纯数字比较（毫秒级），但滚动/重排会反复触发，不优。
- 正确做法：**一次性预计算最优表**（O(N) 一次扫描 6 属性 × 基础/变异两维 = 12 个标量 max + 持有 recId），渲染时 **O(1) 查表**。仅"我的"池参与比较（外部不参与、不显示，避免泄露他人数据规模）。
- 缓存放 LB._cache 级，随数据导入/筛选集合变化失效重建一次；对 2 万级 recs 规模开销可忽略。

UI（用户指定）：
- 命中「全库存最优」的属性数字：**特殊颜色 + 加粗**（基础最优与变异最优各用一色，区别于既有档次染色；例如基础最优金色 `#fbbf24` 加粗、变异最优橙紫系）——不加任何角标/徽章，无新增 DOM 元素。

---

## ⑥ 路线浮窗折叠展开热区被内容遮挡

### 现状
- 线折叠块 `lbRouteLineBlock`（L13993）：互斥 accordion（`LB._routeOpenLine`，L12715），标题条应为整行可点；另有废种折叠（`LB._routeOpenWaste`）。
- 物种选择器等折叠区曾做专门热区处理（v864/865/869 收缩、隐藏），说明折叠热区问题在该浮窗体系有历史。

### 根因（需 DOM 命中复现定位，候选）
- 标题条为普通 `div`，未 `position:relative; z-index` 提升 → body 内子元素（负 margin / 相对定位 / 宽度 100% 的浮层、badge、说明行）在几何上覆盖标题行 → 点击被 body 元素拦截，不触发折叠 toggle。
- 或标题条由箭头+内容多 span 组成，热区只在箭头/窄条而非全栏。

### 方案（仅修热区，禁动样式与结构）

> 用户约束（2026-09-08）：**只允许修复热区（点击触发范围），不允许变动当前样式与 DOM 结构**。

**已定位根因（代码层面）**：`lbRouteLineBlock` 标题行为 `display:flex` 的 div，折叠切换 `onclick="lbRouteLineToggle(idx)"` 只绑在**左侧 flex span**（含箭头+线名，`cursor:pointer`）；右侧**状态徽章**（✓ 齐 / ⚠ 空 / ⚠ 缺♂♀）与标题行边缘空白区不在热区内 → 点击整栏右半不触发；展开后内容区与标题同处一栏视觉下也易误触/漏触。

纯事件层修复（不动样式/结构）：
1. 在**标题行容器 div**（而非左侧 span）上绑定切换：把 `onclick` 移到 flex 外层 div，利用事件冒泡使整行（含右侧 status、边缘）点击均触发；若担心子元素（如未来内容里的按钮）误触，用 `e.target.closest('[data-line]')` 判归属。
2. 展开后的内容区各行不拦截、不绑折叠（内容区在标题行**下方独立 div**，仅需确认其 `onclick` 不冒泡到标题切换即可——若内容区自身无 onclick 则天然不触发）。
3. **复现验证**：浏览器实测——点击标题栏最右空白/状态徽章/文字各处均须折叠切换；展开后点内容区不折叠。若实测仍被几何遮挡（内容区 z 覆盖标题），因禁改样式，需向用户报告该例外再定夺。
4. 折叠记忆/互斥逻辑保持（`LB._routeOpenLine`/`_routeOpenWaste`）不动。

---

## ⑦ 繁育栏筛选结果过多时按年龄阶段折叠（低可视化）

### 现状
- 繁育筛选结果 `lbRenderBreedResult`：我的 / 外部（`isMy`，`lbIsMyRec` L10751）两组，各行带成长徽章（`lbAgeStage` 口径 L10807：婴儿 <0.1 / 幼崽 <0.5 / 青年 <1.0，成年不标；徽章 L10817）。
- 结果多时平铺全列表，无折叠。

### 根因
结果 >10 只时（我的/外部各自计算）一屏放不下，滚动长、难扫；但属性阶段（婴儿/幼崽/青年/成年）天然是分组维度，未利用。

### 方案
1. 每组（我的 / 外部）内当该组结果 **>10 只** 时启用分组折叠；≤10 只维持平铺。
2. 按 `lbAgeStage` 四段分组：婴儿（<0.1）/ 幼崽（<0.5）/ 青年（<1.0）/ 成年（≥1.0）。
3. 折叠态 UI（低可视化）：每段一行小字/小图标 + 计数，如 `🐣 婴儿 ×3 · 幼崽 ×5`，点击该行展开为完整 card 列表（再次点击收起）；整段默认收起，避免结果多时刷屏。
4. 交互细节：
   - 展开状态记忆（与筛选条件绑定，重筛后重置）。
   - 折叠行本身不可再误触为选中操作（`stopPropagation`，参照 v415 既有防误触）。
   - 成年段若大量且无留种价值时，可默认折叠并标注"成年 · N（含废种候选）"。

---

## ⑧ 灭绝玩家冰箱里的南巨被算成野生 —— 根因分析

### 现状
- 归属判定链：`lbIsMyRec(r, playerSet, curPid)`（L10751）→ 容器归类 `lbContainerTypeOf(r)`（L5538）→ `lbRouteMyRecs()` 按 curPid 过滤部落。
- 容器/归属重建链（v545/v821 相关）：cryo 文件 rec 通过 containerId → cryo 哈希 → 设施属主（tribe/facility）回溯玩家；缺失时易落入"无归属"。

### 根因（高概率候选，需数据实证）
1. **Ext cryo rec 缺 tribeId / container 属主解析失败**：南巨在玩家冰箱（cryofridge）的 rec 若 `tribeId=0` 或 containerId 指向的冰箱设施 rec 缺失/未被导入（Ext 文件 schema 旧或该设施未被扫到）→ 回溯不到玩家 → `lbIsMyRec=false`。
2. **数据源判定**：若该 rec 落到 tamed 之外的 wild/unknown 分桶（如 Ext 的 cryo 合并路径未把玩家冰箱球纳入 tamed 源），会被当"野生/外部"。
3. 位置信息：Ext 有海洋/地表多处，坐标在海洋深处 + 未识别容器 → 被归类为野生刷新点附近的可抓野生。

### 验证步骤（先验证后修，禁止直接改判定逻辑）
1. 定位该南巨 rec（物种 Giga / 灭绝服），读其 `tribeId`、`dinoId`、`containerType`、所属文件/分桶、所在 server 文件。
2. 若 `containerType=cryo` 且 `tribeId` 有效 → 查容器回溯是否失败；失败则修 cryo 哈希/设施关联。
3. 若 rec 实际在 cryo 桶但被归 wild → 修分桶/`lbIsMyRec` 归类条件。
4. 修正后重导入验证：筛选/路线浮窗/繁育结果均识别为"我的"，且不影响其他服判定。

### 实证结论（2026-09-08，线上数据 v876，用户复现：以嘎巴丸角色搜 Ext 野生南巨返回 70 只，均为其部落低温冰箱生物）

**复现场景定位 = 「搜野生」链路（非筛选/卡片归属）**：前端 `lbWildSearch` → CF 后端 `dino_backend.py search_wild()`（L597）→ RCON `TransferIdentityFix.WildDinos <species>`（L117）→ 写 `wild_status_{server}.json` → 前端轮询后拉取数据文件 `<server>_wild_<species>.json` 入 `LB._wildCache`。**全程只是转发服端 RCON 返回**。

**实锤证据链（只读拉取线上文件验证）**：
1. `Ext_wild_Gigant_Character_BP.json` 存在且 **count=70**，与用户报告完全一致（savedAt 1788842952580，本次生成）。
2. **数量与来源错位**：前端 recs（tamed 导入）中 Ext 服南巨 `Gigant_Character_BP_C` 仅 **5 只**（player 1 + fridge 4，含板板/JaronV5 部落）；wild 70 只与 recs 交集 **0**——这 70 只**从未被 tamed 数据正确导入**（Ext tamed 导出缺口，与 v542 注释"Ext 服 ArkTamedDinos 导出不全"同源），却出现在搜野生结果里。
3. wild 文件字段含 `ancestors/randomMutations/imprintingQuality/saddle/babyAge` 等**驯养 schema 字段**（服端输出统一字段集），但值全空/0、等级 ≤150、坐标分散多簇 → 输出口径为"野生"（字段来自 CryoFridge 内 cryopod 内容，驯养上下文未透传）。

**根因判定：不在前端、不在 dino_backend（只转发 RCON JSON），而在服端 `TransferIdentityFix.WildDinos` 命令把玩家 CryoFridge（低温冰箱）内 cryopod 内容误纳入 wild 清单返回**；与之同源的还有 **Ext tamed 导出未收录这些冰箱球**（导致"我的"数据缺这 70 只）。

**修复方向（均非 dino-import 代码）**：
- 服端 mod：WildDinos 过滤 CryoFridge/容器内 cryopod 内容（只返回真野外）。
- 服端 tamed 导出：ArkTamedDinos/导出链路需把 CryoFridge 内球纳入 tamed（否则这 70 只在"我的"永远缺失）。
- 若短期内无法改服端：前端可对搜野生结果做**驯养痕迹/容器回查过滤**（拉取结果后剔除与 tamed 同 id 项、或剔除带驯养特征项）作为过渡——但根治在服端。

**待用户裁决的一点**：wild 70 只等级全 ≤150（野生上限），若嘎巴丸冰箱内南巨实际等级 ≤150（低等级收藏/未练级）则完全吻合"冰箱球被当野生"；若冰箱内有 >150 高等级南巨而 wild 无，则需另行核对（可能 wild 只输出部分）。

---

## 实施优先级建议（若后续排期）
| 优先 | 项 | 理由 |
|:---:|---|---|
| P0 | ⑧ Ext 南巨判野 | 数据归属错误影响筛选/路线正确性 |
| P0 | ① 倒计时手动刷新 | 后端已就绪，前端增量小、收益直接 |
| P1 | ⑥ 折叠热区 | 交互缺陷，影响所有线折叠块 |
| P1 | ② 最佳母群公式 | 纯前端公式+提示 |
| P2 | ③ Gen 追踪海图 | 需地图联调验证 |
| P2 | ⑦ 结果年龄折叠 | 纯前端，独立可做 |
| P3 | ④ 颜色稀缺 / ⑤ 最优标注 | 需先定稀有规则/最优口径，属增强 |
